# TRANC3 — Pinned Dependencies
# ──────────────────────────────────────────────────────────────────
# Core web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
python-multipart==0.0.9
websockets==12.0

# AI / ML — Core
torch==2.3.0
transformers==4.41.0
numpy==1.26.4
scipy==1.13.0
langdetect==1.0.9

# AI / ML — Enhanced (MCP + DeepMind + TF)
sentence-transformers==3.0.0        # semantic skill search
sse-starlette==2.1.0                # MCP SSE transport
tokenizers==0.19.1                  # Tranc3 BPE tokenizer (Rust backend — NOT a pretrained model)
langchain==0.2.5                    # workflow/chain integration
langchain-community==0.2.5
faiss-cpu==1.8.0                    # fast similarity search
jmespath==1.0.1                     # data transform in workflow nodes
# tensorflow==2.16.1                # optional: uncomment for TF hybrid engine
# keras==3.3.3                      # optional: required with tensorflow
# anthropic==0.28.0                 # optional: NOT used by Tranc3 — external API removed
# langchain-anthropic==0.1.15       # optional: only needed if re-enabling Anthropic integration

# DeepMind / Planning / RL
gymnasium==0.29.1                   # RL environment interface
optuna==3.6.1                       # hyperparameter optimisation

# Quantum
qiskit==1.1.1
qiskit-aer==0.14.2
qiskit-nature==0.7.2

# Logging / Observability
python-json-logger==2.0.7
opentelemetry-sdk==1.24.0
opentelemetry-instrumentation-fastapi==0.45b0
prometheus-client==0.20.0
prometheus-fastapi-instrumentator==7.0.0
langfuse==2.30.0                    # LLM observability for workflow nodes
structlog==24.1.0

# Database
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9
supabase==2.4.6
qdrant-client==1.9.1                # vector DB for skills + embeddings

# Cache
redis==5.0.4
upstash-redis==1.1.0

# Auth & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==42.0.7

# HTTP
httpx==0.27.0
aiohttp==3.9.5

# Utilities
python-dotenv==1.0.1
pyyaml==6.0.1
tenacity==8.3.0
toml==0.10.2

# Payments
stripe==9.9.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
