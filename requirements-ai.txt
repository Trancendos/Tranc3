# TRANC3 — Extended AI/ML Dependencies (Optional)
# Only install if you need LangChain, Transformers, Qiskit, or LangFuse
# Updated: 2026-06 — CVE Remediation + New Zero-Cost AI/ML Tools

# === AI Orchestration ===
# Fixes: CVE-2024-7042 (SQL injection), CVE-2024-7774 (SSRF),
#        CVE-2024-58340 (deserialization), CVE-2024-5998 (XSS in agent output),
#        CVE-2024-3095 (prompt injection), CVE-2026-41481 (data exfiltration),
#        CVE-2026-41488 (tool execution), CVE-2026-45134 (arbitrary code exec)
langchain==1.3.1

# Fixes: CVE-2025-6984 (prompt injection in community tools)
langchain-community==0.4.1

# === NLP / Model Hub ===
# Fixes: CVE-2024-11394, CVE-2024-11393, CVE-2024-11392 (unsafe deserialization),
#        CVE-2026-1839 (pickle RCE), CVE-2025-6638 (path traversal),
#        CVE-2025-2099 (model loading RCE), CVE-2025-6921 (arbitrary file write),
#        CVE-2024-12720 (tokenization overflow), CVE-2025-1194 (DoS),
#        CVE-2025-3264, CVE-2025-3263, CVE-2025-3933, CVE-2025-5197,
#        CVE-2025-6051, CVE-2025-3777
transformers==5.8.1

# === Quantum Computing ===
# Fixes: CVE-2025-2000 (RCE), CVE-2025-1403 (information disclosure)
qiskit==2.4.1

# === Observability ===
# Fixes: CVE-2025-9799 (SSRF in LangFuse)
langfuse==4.6.1

# === Zero-Cost Local Inference ===
# llama-cpp-python: Run GGUF quantized models (Llama, Mistral, Phi) locally for free.
# Supports CPU (Oracle free ARM works — slow but free) and CUDA if available.
# 7B model quantized to Q4_K_M runs in ~6GB RAM, ~0.5–2 tokens/sec on Oracle ARM.
# HONEST: CPU inference is very slow — only viable for batch use or Oracle free VM.
llama-cpp-python==0.3.9

# === Vector Database (Self-Hosted, Genuinely Free) ===
# ChromaDB: Local SQLite-backed vector store. No server needed in dev.
# Production: runs with PostgreSQL backend (Supabase free tier or self-hosted Postgres).
# Use for: RAG pipelines, semantic search, context memory.
# GHSA-f4j7-r4q5-qw2c / CVE-2026-45829: pre-auth code injection when trust_remote_code=true
# on Chroma server collections API. No patched PyPI release yet (last_affected 1.5.9).
# Mitigation: embedded PersistentClient/Client only (vector_plan_cache.py); never enable
# trust_remote_code; do not expose Chroma HTTP server to untrusted networks.
chromadb==1.5.9

# === Structured LLM Outputs ===
# instructor: Pydantic-validated structured outputs from any LLM API.
# Free to use, works with all providers in our rotation.
instructor==1.8.3

# === Fine-Tuning (Zero-Cost via Kaggle/Colab) ===
# peft: LoRA/QLoRA/IA3 adapters. Run fine-tuning on Kaggle free 30 hrs/week.
# Honest: training FROM SCRATCH at zero cost is not feasible. Fine-tuning is.
peft==0.15.2

# bitsandbytes: 4-bit/8-bit quantization for fitting larger models in less VRAM.
# Required for QLoRA. Only useful if running on a machine with a CUDA GPU.
# HONEST: this package provides zero benefit on CPU-only environments.
bitsandbytes==0.47.0

# unsloth: 2-5x faster LoRA/QLoRA training. Verified speedups, not marketing.
# Only for Llama/Mistral/Phi model families on CUDA. CPU: no benefit.
# unsloth==2025.6.10  # Uncomment when fine-tuning — large install
