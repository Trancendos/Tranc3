# deploy/forgejo/docker-compose.yml
# The Workshop — Forgejo + act runner (self-hosted CI/CD).
#
# Deploy:
#   docker compose -f deploy/forgejo/docker-compose.yml up -d
#
# Available at: https://trancendos.com/the-workshop
# Runner: registers automatically once RUNNER_REGISTRATION_TOKEN is set.
#
# Get the token after first Forgejo boot:
#   Site Admin → Management → Runners → Create new runner → copy token
#   Then: RUNNER_REGISTRATION_TOKEN=<token> docker compose up -d act-runner

version: "3.9"

services:
  forgejo:
    image: codeberg.org/forgejo/forgejo:7
    container_name: the-workshop
    restart: unless-stopped
    environment:
      - USER_UID=1000
      - USER_GID=1000
      - FORGEJO__server__ROOT_URL=https://trancendos.com/the-workshop
      - FORGEJO__server__HTTP_PORT=3000
      - FORGEJO__server__DOMAIN=trancendos.com
      - FORGEJO__server__SSH_DOMAIN=trancendos.com
      - FORGEJO__server__SSH_PORT=2222
      - FORGEJO__server__DISABLE_SSH=false
      - FORGEJO__server__LFS_START_SERVER=true
      - FORGEJO__database__DB_TYPE=sqlite3
      - FORGEJO__database__PATH=/data/forgejo/forgejo.db
      - FORGEJO__repository__DEFAULT_BRANCH=main
      - FORGEJO__service__DISABLE_REGISTRATION=false
      - FORGEJO__service__REQUIRE_SIGNIN_VIEW=false
      - FORGEJO__log__LEVEL=Info
      - FORGEJO__security__INSTALL_LOCK=false
      # Enable Forgejo Actions
      - FORGEJO__actions__ENABLED=true
    ports:
      - "127.0.0.1:3456:3000"
      - "2222:22"
    volumes:
      - forgejo-data:/data
      - /etc/timezone:/etc/timezone:ro
      - /etc/localtime:/etc/localtime:ro
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/the-workshop/-/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  act-runner:
    image: code.forgejo.org/forgejo/runner:3
    container_name: the-workshop-runner
    restart: unless-stopped
    depends_on:
      forgejo:
        condition: service_healthy
    environment:
      - FORGEJO_INSTANCE_URL=http://forgejo:3000/the-workshop
      - FORGEJO_RUNNER_REGISTRATION_TOKEN=${RUNNER_REGISTRATION_TOKEN:-}
      - FORGEJO_RUNNER_NAME=trancendos-runner
      - FORGEJO_RUNNER_LABELS=self-hosted,linux,x64
    volumes:
      - act-runner-data:/data
      # Docker socket so runner can build/run containers
      - /var/run/docker.sock:/var/run/docker.sock
    # Runner needs wrangler + flyctl available — install via runner config
    # or build a custom image (see scripts/runner-dockerfile)

volumes:
  forgejo-data:
    name: forgejo-data
  act-runner-data:
    name: act-runner-data
