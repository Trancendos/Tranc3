global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: tranc3-api
    static_configs:
      - targets: ['api:8000']
    metrics_path: /metrics

rule_files:
  - "prometheus-alerts.yml"
