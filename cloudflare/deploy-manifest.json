{
  "_comment": [
    "Data-driven Cloudflare deploy manifest. Single source of truth for which",
    "workers the deploy pipeline knows about. Consumed by",
    ".forgejo/scripts/cf_deploy_plan.py, which the .forgejo/workflows/deploy-cloudflare.yml",
    "workflow runs to compute a per-worker deploy matrix from the files that",
    "actually changed. To onboard a new worker: add an entry here (name = the",
    "wrangler.toml `name`, dir = its folder under cloudflare/). No workflow edit needed.",
    "health_url is polled after deploy as a SOFT signal (warning only, never fails",
    "the job — `wrangler deploy` exit code is the authoritative success gate). Leave",
    "it \"\" to skip the health poll for that worker."
  ],
  "workers": [
    { "name": "trancendos-api-gateway", "dir": "trancendos-api-gateway", "health_url": "https://api.trancendos.com/health" },
    { "name": "tranc3-ai",              "dir": "tranc3-ai",              "health_url": "https://tranc3-ai.luminous-aimastermind.workers.dev/health" },
    { "name": "infinity-void",          "dir": "infinity-void",          "health_url": "https://infinity-void.luminous-aimastermind.workers.dev/health" },
    { "name": "tranc3-notifications",   "dir": "notifications-rotation", "health_url": "" },
    { "name": "tranc3-queue",           "dir": "queue-rotation",         "health_url": "" },
    { "name": "tranc3-search",          "dir": "search-rotation",        "health_url": "" },
    { "name": "tranc3-storage",         "dir": "storage-rotation",       "health_url": "" }
  ]
}
