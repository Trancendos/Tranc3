# docker-compose.production.yml
# Tranc3 Full Production Stack — Self-Hosted, Zero-Cost Architecture
#
# Replaces ALL Cloudflare Worker dependencies with self-hosted Python/FastAPI workers.
# Includes observability stack (Prometheus, Grafana, Loki), reverse proxy (Traefik),
# secrets management (Vault), and IPFS for distributed storage.
#
# Usage:
#   docker compose -f docker-compose.production.yml up -d
#
# Architecture:
#   - Traefik: Reverse proxy + auto-discovery (replaces CF routing)
#   - 29 FastAPI workers: Self-hosted Python services (replaces CF Workers)
#   - Prometheus + Grafana + Loki: Observability stack (replaces CF analytics)
#   - Vault: Secrets management (replaces CF environment variables)
#   - IPFS: Distributed storage (replaces CF R2)
#
# Zero external provider dependencies. Zero cost.

version: "3.8"

# ---------------------------------------------------------------------------
# Shared configuration
# ---------------------------------------------------------------------------
x-common: &common
  restart: unless-stopped
  networks:
    - tranc3-net

x-worker-common: &worker-common
  <<: *common
  env_file:
    - .env.production
  logging:
    driver: json-file
    options:
      max-size: "10m"
      max-file: "3"
  labels:
    - "tranc3.stack=production"

x-healthcheck-default: &healthcheck-default
  interval: 30s
  timeout: 5s
  retries: 3
  start_period: 10s

# ---------------------------------------------------------------------------
# Services
# ---------------------------------------------------------------------------
services:

  # ═══════════════════════════════════════════════════════════════════════
  # INFRASTRUCTURE LAYER
  # ═══════════════════════════════════════════════════════════════════════

  # ── Traefik — Reverse Proxy & Load Balancer ──────────────────────────
  # Replaces: Cloudflare routing, CF Load Balancing
  traefik:
    image: traefik:v3.0
    container_name: tranc3-traefik
    <<: *common
    command:
      - "--api.dashboard=true"
      - "--providers.docker=true"
      - "--providers.docker.exposedbydefault=false"
      - "--providers.docker.network=tranc3-net"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.websecure.address=:443"
      - "--entrypoints.metrics.address=:9090"
      - "--metrics.prometheus=true"
      - "--metrics.prometheus.entrypoint=metrics"
      - "--log.level=INFO"
      - "--accesslog=true"
      - "--accesslog.format=json"
    ports:
      - "80:80"
      - "443:443"
      - "8888:8080"   # Traefik dashboard (moved from 8080 to avoid KrakenD conflict)
      - "9090:9090"   # Metrics
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - traefik-config:/etc/traefik
      - traefik-certs:/letsencrypt
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.traefik-dashboard.rule=Host(`traefik.trancendos.local`)"
      - "traefik.http.routers.traefik-dashboard.entrypoints=web"
      - "traefik.http.routers.traefik-dashboard.service=api@internal"
    healthcheck:
      test: ["CMD", "traefik", "healthcheck", "--ping"]
      <<: *healthcheck-default

  # ── Vault — Secrets Management ───────────────────────────────────────
  # Replaces: Cloudflare environment variables, CF Secrets
  vault:
    image: hashicorp/vault:1.15
    container_name: tranc3-vault
    <<: *common
    command: server -config=/vault/config/vault.hcl
    cap_add:
      - IPC_LOCK
    environment:
      - VAULT_ADDR=http://127.0.0.1:8200
    ports:
      - "8200:8200"
    volumes:
      - vault-data:/vault/data
      - ./deploy/vault/vault.hcl:/vault/config/vault.hcl:ro
    healthcheck:
      test: ["CMD-SHELL", "vault status >/dev/null 2>&1 || [ $$? -eq 2 ]"]
      <<: *healthcheck-default

  # ── Prometheus — Metrics Collection ──────────────────────────────────
  # Replaces: Cloudflare Analytics, CF Worker Analytics
  prometheus:
    image: prom/prometheus:v2.50.0
    container_name: tranc3-prometheus
    <<: *common
    ports:
      - "9091:9090"
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./monitoring/alerts:/etc/prometheus/alerts:ro
      - prometheus-data:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.retention.time=30d"
      - "--storage.tsdb.retention.size=5GB"
      - "--web.enable-lifecycle"
      - "--web.enable-admin-api"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:9090/-/healthy"]
      <<: *healthcheck-default

  # ── Alertmanager — Alert Routing & Notifications ──────────────────────
  alertmanager:
    image: prom/alertmanager:v0.27.0
    container_name: tranc3-alertmanager
    <<: *common
    ports:
      - "9093:9093"
    volumes:
      - ./monitoring/alertmanager.yml:/etc/alertmanager/alertmanager.yml.tmpl:ro
      - alertmanager-data:/alertmanager
    environment:
      - ALERTMANAGER_SLACK_WEBHOOK=${ALERTMANAGER_SLACK_WEBHOOK:-}
    entrypoint:
      - /bin/sh
      - -c
      - "envsubst < /etc/alertmanager/alertmanager.yml.tmpl > /tmp/alertmanager.yml && exec /bin/alertmanager --config.file=/tmp/alertmanager.yml --storage.path=/alertmanager --web.external-url=http://localhost:9093"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:9093/-/healthy"]
      <<: *healthcheck-default

  # ── OpenTelemetry Collector — Unified Telemetry Pipeline ──────────────
  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.96.0
    container_name: tranc3-otel-collector
    <<: *common
    ports:
      - "4317:4317"   # OTLP gRPC
      - "4318:4318"   # OTLP HTTP
      - "8889:8888"   # Collector self-metrics (host:8889 avoids Traefik port 8888 conflict)
    volumes:
      - ./monitoring/otel-collector.yml:/etc/otel-collector.yml:ro
    command: ["--config=/etc/otel-collector.yml"]
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8888/metrics"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ── Grafana — Observability Dashboard ────────────────────────────────
  # Replaces: Cloudflare Dashboard, CF Analytics
  grafana:
    image: grafana/grafana:10.3.0
    container_name: tranc3-grafana
    <<: *common
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:?required}
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_SERVER_ROOT_URL=http://localhost:3001
      - GF_ANALYTICS_CHECK_FOR_UPDATES=false
    ports:
      - "3001:3000"
    volumes:
      - grafana-data:/var/lib/grafana
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
    depends_on:
      - prometheus
      - loki
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3000/api/health"]
      <<: *healthcheck-default

  # ── Loki — Log Aggregation ───────────────────────────────────────────
  # Replaces: Cloudflare Logs, CF Logpush
  loki:
    image: grafana/loki:2.9.0
    container_name: tranc3-loki
    <<: *common
    ports:
      - "3100:3100"
    volumes:
      - ./monitoring/loki.yml:/etc/loki/local-config.yaml:ro
      - loki-data:/loki
    command: -config.file=/etc/loki/local-config.yaml
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3100/ready"]
      <<: *healthcheck-default

  # ── Promtail — Log Shipper ───────────────────────────────────────────
  promtail:
    image: grafana/promtail:2.9.0
    container_name: tranc3-promtail
    <<: *common
    volumes:
      - ./monitoring/promtail.yml:/etc/promtail/config.yml:ro
      - /var/log:/var/log:ro
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
    command: -config.file=/etc/promtail/config.yml
    depends_on:
      - loki
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3100/ready"]
      <<: *healthcheck-default

  # ── VictoriaMetrics — Prometheus-Compatible TSDB ─────────────────────
  # Apache 2.0. Drop-in Prometheus replacement with 10× less RAM and disk.
  # Scrapes same targets as Prometheus; Grafana talks to both.
  # Query API: http://victoriametrics:8428  (Prometheus-compatible PromQL)
  victoriametrics:
    image: victoriametrics/victoria-metrics:latest
    container_name: tranc3-victoriametrics
    <<: *common
    command:
      - "--storageDataPath=/storage"
      - "--retentionPeriod=30d"
      - "--httpListenAddr=:8428"
      - "--promscrape.config=/etc/prometheus/prometheus.yml"
    ports:
      - "8428:8428"
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - victoriametrics-data:/storage
    depends_on:
      - prometheus
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8428/health"]
      <<: *healthcheck-default

  # ── Valkey — BSD-3 Redis-Compatible Key-Value Store ───────────────────
  # Drop-in Redis replacement (same redis-py client, same port 6379).
  # BSD-3 licence; community fork of Redis 7.2 under Linux Foundation.
  valkey:
    image: valkey/valkey:8-alpine
    container_name: tranc3-valkey
    <<: *common
    command: ["valkey-server", "--appendonly", "yes", "--save", "60", "1"]
    ports:
      - "6379:6379"
    volumes:
      - valkey-data:/data
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 15s
      timeout: 5s
      retries: 3

  # ── Zot — OCI Container Registry ─────────────────────────────────────
  # Apache 2.0. Prevents Docker Hub rate-limiting for the platform.
  # All workers can pull images from http://zot:5000 inside the network.
  zot:
    image: ghcr.io/project-zot/zot-linux-amd64:latest
    container_name: tranc3-zot
    <<: *common
    ports:
      - "5000:5000"
    volumes:
      - ./config/zot/config.json:/etc/zot/config.json:ro
      - zot-data:/var/lib/registry
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:5000/v2/"]
      interval: 30s
      timeout: 5s
      retries: 3

  # ── IPFS — Distributed Storage ───────────────────────────────────────
  # Replaces: Cloudflare R2, CF IPFS Gateway
  ipfs:
    image: ipfs/kubo:v0.28.0
    container_name: tranc3-ipfs
    <<: *common
    ports:
      - "4001:4001"   # Swarm
      - "5001:5001"   # API
      - "8081:8080"   # Gateway
    volumes:
      - ipfs-data:/data/ipfs
    healthcheck:
      test: ["CMD", "ipfs", "id"]
      <<: *healthcheck-default

  # ── Watchtower — Zero-Cost Auto-Update (Apache 2.0) ─────────────────
  # Polls Docker Hub every 5 minutes; only updates containers labelled
  # com.centurylinklabs.watchtower.enable=true
  # Sends Slack/webhook notification on update if WATCHTOWER_NOTIFICATION_URL set.
  watchtower:
    image: containrrr/watchtower:1.7.1
    container_name: tranc3-watchtower
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    command: --interval 300 --cleanup --label-enable --rolling-restart
    environment:
      - WATCHTOWER_NOTIFICATIONS_LEVEL=info
      - WATCHTOWER_NOTIFICATION_URL=${WATCHTOWER_NOTIFICATION_URL:-}
    networks:
      - tranc3-net
    labels:
      - "traefik.enable=false"

  # ── NATS — High-Throughput Inter-Worker Messaging (Apache 2.0) ───────
  # Lightweight alternative to Redis pub/sub for high-volume event streams.
  # JetStream enabled for durable queues (replaces SQLite queue for >1k msg/s).
  # 15MB binary, 1M+ msg/s throughput.
  nats:
    image: nats:2.10-alpine
    container_name: tranc3-nats
    restart: unless-stopped
    command: ["-js", "-sd", "/data", "--http_port", "8222"]
    ports:
      - "4222:4222"   # Client connections
      - "8222:8222"   # HTTP monitoring
      - "6222:6222"   # Cluster routing (future)
    volumes:
      - nats-data:/data
    networks:
      - tranc3-net
    labels:
      - "traefik.enable=false"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8222/healthz"]
      interval: 15s
      timeout: 5s
      retries: 3

  # ── Grafana Tempo — Distributed Tracing Backend ───────────────────────
  # Receives OTLP spans from workers via OpenTelemetry exporter.
  # Integrates with existing Grafana (auto-datasource provisioned).
  tempo:
    image: grafana/tempo:2.3.0
    container_name: tranc3-tempo
    restart: unless-stopped
    command: ["-config.file=/etc/tempo/config.yml"]
    volumes:
      - tempo-data:/tmp/tempo
      - ./monitoring/tempo.yml:/etc/tempo/config.yml:ro
    ports:
      - "3200:3200"     # HTTP query API
      - "14317:4317"    # OTLP gRPC receiver (host 14317 → container 4317; avoids conflict with otel-collector)
      - "14318:4318"    # OTLP HTTP receiver (host 14318 → container 4318)
    networks:
      - tranc3-net
    labels:
      - "traefik.enable=false"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3200/ready"]
      interval: 15s
      timeout: 5s
      retries: 3

  # ═══════════════════════════════════════════════════════════════════════
  # P0 WORKERS — Critical Path
  # ═══════════════════════════════════════════════════════════════════════

  # ── Tranc3 Backend — Core FastAPI App (Port 8000) ────────────────────
  tranc3-backend:
    <<: *worker-common
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tranc3-backend
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - JWT_SECRET=${JWT_SECRET}
      - DATABASE_URL=${DATABASE_URL}
      - REDIS_URL=${REDIS_URL}
      - ENVIRONMENT=production
      - CORS_ORIGINS=${CORS_ORIGINS}
      - OLLAMA_URL=${OLLAMA_URL:-http://ollama:11434}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-}
      - HF_API_KEY=${HF_API_KEY:-}
    ports:
      - "8000:8000"
    volumes:
      - backend-data:/app/data
      - backend-models:/app/models
      - backend-cache:/app/cache
      - backend-logs:/app/logs
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.tranc3-backend.rule=PathPrefix(`/api/backend`) || PathPrefix(`/mcp`)"
      - "traefik.http.routers.tranc3-backend.entrypoints=web"
      - "traefik.http.services.tranc3-backend.loadbalancer.server.port=8000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      <<: *healthcheck-default

  # ── Tranc3 AI — The Spark (Port 8001) ────────────────────────────────
  tranc3-ai:
    <<: *worker-common
    build:
      context: ./workers/tranc3-ai
      dockerfile: Dockerfile
    container_name: tranc3-ai-worker
    environment:
      - TRANC3_BACKEND_URL=http://tranc3-backend:8000
      - TRANC3_NANO_URL=http://tranc3-backend:8001
      - TRANC3_AUTH_URL=${TRANC3_AUTH_URL:-}
      - ENVIRONMENT=production
    ports:
      - "8001:8001"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.tranc3-ai.rule=PathPrefix(`/api/ai`)"
      - "traefik.http.routers.tranc3-ai.entrypoints=web"
      - "traefik.http.services.tranc3-ai.loadbalancer.server.port=8001"
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8001/health', timeout=5).read()\""]
      <<: *healthcheck-default
    depends_on:
      - tranc3-backend

  # ── The Void — Secrets Vault (Port 8002) ────────────────────────────
  infinity-void:
    <<: *worker-common
    build:
      context: ./workers/infinity-void
      dockerfile: Dockerfile
    container_name: infinity-void-worker
    environment:
      - MASTER_KEY_SEED=${MASTER_KEY_SEED}
      - INTERNAL_SECRET=${INTERNAL_SECRET}
      - ENVIRONMENT=production
    ports:
      - "8002:8002"
    volumes:
      - void-data:/data
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.infinity-void.rule=PathPrefix(`/api/void`)"
      - "traefik.http.routers.infinity-void.entrypoints=web"
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8002/health', timeout=5).read()\""]
      <<: *healthcheck-default

  # ── API Gateway (Port 8003) ─────────────────────────────────────────
  api-gateway:
    <<: *worker-common
    build:
      context: ./workers/api-gateway
      dockerfile: Dockerfile
    container_name: trancendos-api-gateway-worker
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - INTERNAL_SECRET=${INTERNAL_SECRET}
      - AUTH_SERVICE_URL=http://infinity-auth:8005
      - USERS_SERVICE_URL=http://users-service:8006
      - PRODUCTS_SERVICE_URL=http://products-service:8011
      - ORDERS_SERVICE_URL=http://orders-service:8012
      - PAYMENTS_SERVICE_URL=http://payments-service:8013
      - TRANC3_AI_SERVICE_URL=http://tranc3-ai:8001
      - ENVIRONMENT=production
    ports:
      - "8003:8003"
    depends_on:
      - tranc3-ai
      - infinity-auth
      - users-service
      - products-service
      - orders-service
      - payments-service
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.api-gateway.rule=PathPrefix(`/api/`)"
      - "traefik.http.routers.api-gateway.entrypoints=web"
      - "traefik.http.routers.api-gateway.priority=1"
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8003/health', timeout=5).read()\""]
      <<: *healthcheck-default

  # ── The Nexus — WebSocket API (Port 8004) ──────────────────────────
  infinity-ws:
    <<: *worker-common
    build:
      context: ./workers/infinity-ws
      dockerfile: Dockerfile
    container_name: infinity-ws-worker
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - ENVIRONMENT=production
    ports:
      - "8004:8004"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.infinity-ws.rule=PathPrefix(`/ws/`)"
      - "traefik.http.routers.infinity-ws.entrypoints=web"
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8004/health', timeout=5).read()\""]
      <<: *healthcheck-default

  # ── Infinity Auth — OAuth2/SSO (Port 8005) ──────────────────────────
  infinity-auth:
    <<: *worker-common
    build:
      context: ./workers/infinity-auth
      dockerfile: Dockerfile
    container_name: infinity-auth-worker
    environment:
      - JWT_SECRET=${JWT_SECRET}
      - MASTER_KEY_SEED=${MASTER_KEY_SEED}
      - ENVIRONMENT=production
    ports:
      - "8005:8005"
    volumes:
      - auth-data:/data
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.infinity-auth.rule=PathPrefix(`/auth/`)"
      - "traefik.http.routers.infinity-auth.entrypoints=web"
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8005/health', timeout=5).read()\""]
      <<: *healthcheck-default

  # ═══════════════════════════════════════════════════════════════════════
  # P1 WORKERS — Core Services
  # ═══════════════════════════════════════════════════════════════════════

  # ── Users Service (Port 8006) ────────────────────────────────────────
  users-service:
    <<: *worker-common
    build:
      context: ./workers/users-service
      dockerfile: Dockerfile
    container_name: users-service-worker
    ports:
      - "8006:8006"
    volumes:
      - users-data:/data
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8006/health', timeout=5).read()\""]
      <<: *healthcheck-default

  # ── The Observatory — Monitoring Dashboard (Port 8007) ──────────────
  monitoring:
    <<: *worker-common
    build:
      context: ./workers/monitoring
      dockerfile: Dockerfile
    container_name: monitoring-worker
    ports:
      - "8007:8007"
    volumes:
      - monitoring-data:/data
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8007/health', timeout=5).read()\""]
      <<: *healthcheck-default

  # ── Notifications Service (Port 8008) ───────────────────────────────
  notifications:
    <<: *worker-common
    build:
      context: ./workers/notifications
      dockerfile: Dockerfile
    container_name: notifications-worker
    ports:
      - "8008:8008"
    volumes:
      - notifications-data:/data
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8008/health', timeout=5).read()\""]
      <<: *healthcheck-default

  # ── Infinity AI — AI API Gateway (Port 8009) ────────────────────────
  infinity-ai:
    <<: *worker-common
    build:
      context: ./workers/infinity-ai
      dockerfile: Dockerfile
    container_name: infinity-ai-worker
    environment:
      - OLLAMA_BASE_URL=http://ollama:11434
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-}
      - HUGGINGFACE_API_KEY=${HUGGINGFACE_API_KEY:-}
      - ENVIRONMENT=production
    ports:
      - "8009:8009"
    volumes:
      - ai-data:/data
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8009/health', timeout=5).read()\""]
      <<: *healthcheck-default

  # ═══════════════════════════════════════════════════════════════════════
  # P2 WORKERS — Domain Services
  # ═══════════════════════════════════════════════════════════════════════

  # ── The Digital Grid — Workflow Orchestration (Port 8010) ────────────
  the-grid:
    <<: *worker-common
    build:
      context: ./workers/the-grid
      dockerfile: Dockerfile
    container_name: the-grid-worker
    ports:
      - "8010:8010"
    volumes:
      - grid-data:/data
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8010/health', timeout=5).read()\""]
      <<: *healthcheck-default

  # ── Products Service (Port 8011) ─────────────────────────────────────
  products-service:
    <<: *worker-common
    build:
      context: ./workers/products-service
      dockerfile: Dockerfile
    container_name: products-service-worker
    ports:
      - "8011:8011"
    volumes:
      - products-data:/data
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8011/health', timeout=5).read()\""]
      <<: *healthcheck-default

  # ── Orders Service (Port 8012) ───────────────────────────────────────
  orders-service:
    <<: *worker-common
    build:
      context: ./workers/orders-service
      dockerfile: Dockerfile
    container_name: orders-service-worker
    ports:
      - "8012:8012"
    volumes:
      - orders-data:/data
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8012/health', timeout=5).read()\""]
      <<: *healthcheck-default

  # ── Payments Service (Port 8013) ─────────────────────────────────────
  payments-service:
    <<: *worker-common
    build:
      context: ./workers/payments-service
      dockerfile: Dockerfile
    container_name: payments-service-worker
    ports:
      - "8013:8013"
    volumes:
      - payments-data:/data
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8013/health', timeout=5).read()\""]
      <<: *healthcheck-default

  # ── Files Service (Port 8014) ────────────────────────────────────────
  files-service:
    <<: *worker-common
    build:
      context: ./workers/files-service
      dockerfile: Dockerfile
    container_name: files-service-worker
    ports:
      - "8014:8014"
    volumes:
      - files-data:/data
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8014/health', timeout=5).read()\""]
      <<: *healthcheck-default

  # ── Identity Service (Port 8015) ─────────────────────────────────────
  identity-service:
    <<: *worker-common
    build:
      context: ./workers/identity-service
      dockerfile: Dockerfile
    container_name: identity-service-worker
    ports:
      - "8015:8015"
    volumes:
      - identity-data:/data
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8015/health', timeout=5).read()\""]
      <<: *healthcheck-default

  # ═══════════════════════════════════════════════════════════════════════
  # P3 WORKERS — Stub Services (TODO: Full Implementation)
  # ═══════════════════════════════════════════════════════════════════════

  analytics-service:
    <<: *worker-common
    build: { context: ./workers/analytics-service, dockerfile: Dockerfile }
    container_name: analytics-service-worker
    ports: ["8016:8016"]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8016/health', timeout=5).read()\""]
      <<: *healthcheck-default

  search-service:
    <<: *worker-common
    build: { context: ./workers/search-service, dockerfile: Dockerfile }
    container_name: search-service-worker
    ports: ["8017:8017"]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8017/health', timeout=5).read()\""]
      <<: *healthcheck-default

  email-service:
    <<: *worker-common
    build: { context: ./workers/email-service, dockerfile: Dockerfile }
    container_name: email-service-worker
    ports: ["8018:8018"]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8018/health', timeout=5).read()\""]
      <<: *healthcheck-default

  sms-service:
    <<: *worker-common
    build: { context: ./workers/sms-service, dockerfile: Dockerfile }
    container_name: sms-service-worker
    ports: ["8019:8019"]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8019/health', timeout=5).read()\""]
      <<: *healthcheck-default

  storage-service:
    <<: *worker-common
    build: { context: ./workers/storage-service, dockerfile: Dockerfile }
    container_name: storage-service-worker
    ports: ["8020:8020"]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8020/health', timeout=5).read()\""]
      <<: *healthcheck-default

  cron-service:
    <<: *worker-common
    build: { context: ./workers/cron-service, dockerfile: Dockerfile }
    container_name: cron-service-worker
    ports: ["8021:8021"]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8021/health', timeout=5).read()\""]
      <<: *healthcheck-default

  queue-service:
    <<: *worker-common
    build: { context: ./workers/queue-service, dockerfile: Dockerfile }
    container_name: queue-service-worker
    ports: ["8022:8022"]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8022/health', timeout=5).read()\""]
      <<: *healthcheck-default

  cache-service:
    <<: *worker-common
    build: { context: ./workers/cache-service, dockerfile: Dockerfile }
    container_name: cache-service-worker
    ports: ["8023:8023"]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8023/health', timeout=5).read()\""]
      <<: *healthcheck-default

  config-service:
    <<: *worker-common
    build: { context: ./workers/config-service, dockerfile: Dockerfile }
    container_name: config-service-worker
    ports: ["8024:8024"]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8024/health', timeout=5).read()\""]
      <<: *healthcheck-default

  audit-service:
    <<: *worker-common
    build: { context: ./workers/audit-service, dockerfile: Dockerfile }
    container_name: audit-service-worker
    ports: ["8025:8025"]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8025/health', timeout=5).read()\""]
      <<: *healthcheck-default

  rate-limit-service:
    <<: *worker-common
    build: { context: ./workers/rate-limit-service, dockerfile: Dockerfile }
    container_name: rate-limit-service-worker
    ports: ["8026:8026"]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8026/health', timeout=5).read()\""]
      <<: *healthcheck-default

  geo-service:
    <<: *worker-common
    build: { context: ./workers/geo-service, dockerfile: Dockerfile }
    container_name: geo-service-worker
    ports: ["8027:8027"]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8027/health', timeout=5).read()\""]
      <<: *healthcheck-default

  cdn-service:
    <<: *worker-common
    build: { context: ./workers/cdn-service, dockerfile: Dockerfile }
    container_name: cdn-service-worker
    ports: ["8028:8028"]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8028/health', timeout=5).read()\""]
      <<: *healthcheck-default

  health-aggregator:
    <<: *worker-common
    build: { context: ./workers/health-aggregator, dockerfile: Dockerfile }
    container_name: health-aggregator-worker
    ports: ["8029:8029"]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8029/health', timeout=5).read()\""]
      <<: *healthcheck-default

  # ── P3 — Intelligence / AI Layer (8030–8038) ─────────────────────────────

  gbrain-bridge:
    <<: *worker-common
    build: { context: ./workers/gbrain-bridge, dockerfile: Dockerfile }
    container_name: gbrain-bridge-worker
    ports: ["8030:8030"]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8030/health', timeout=5).read()\""]
      <<: *healthcheck-default

  topology-service:
    <<: *worker-common
    build: { context: ./workers/topology-service, dockerfile: Dockerfile }
    container_name: topology-service-worker
    ports: ["8031:8031"]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8031/health', timeout=5).read()\""]
      <<: *healthcheck-default

  ledger-service:
    <<: *worker-common
    build: { context: ./workers/ledger-service, dockerfile: Dockerfile }
    container_name: ledger-service-worker
    ports: ["8032:8032"]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8032/health', timeout=5).read()\""]
      <<: *healthcheck-default

  model-router-service:
    <<: *worker-common
    build: { context: ./workers/model-router-service, dockerfile: Dockerfile }
    container_name: model-router-service-worker
    ports: ["8033:8033"]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8033/health', timeout=5).read()\""]
      <<: *healthcheck-default

  workflow-engine-service:
    <<: *worker-common
    build: { context: ./workers/workflow-engine-service, dockerfile: Dockerfile }
    container_name: workflow-engine-service-worker
    ports: ["8034:8034"]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8034/health', timeout=5).read()\""]
      <<: *healthcheck-default

  skills-benchmark-service:
    <<: *worker-common
    build: { context: ./workers/skills-benchmark-service, dockerfile: Dockerfile }
    container_name: skills-benchmark-service-worker
    ports: ["8035:8035"]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8035/health', timeout=5).read()\""]
      <<: *healthcheck-default

  langchain-integration-service:
    <<: *worker-common
    build: { context: ./workers/langchain-integration-service, dockerfile: Dockerfile }
    container_name: langchain-integration-service-worker
    ports: ["8036:8036"]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8036/health', timeout=5).read()\""]
      <<: *healthcheck-default

  deepagents-orchestrator-service:
    <<: *worker-common
    build: { context: ./workers/deepagents-orchestrator-service, dockerfile: Dockerfile }
    container_name: deepagents-orchestrator-service-worker
    ports: ["8037:8037"]
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8037/health', timeout=5).read()\""]
      <<: *healthcheck-default

  vault-service:
    <<: *worker-common
    build: { context: ./workers/vault-service, dockerfile: Dockerfile }
    container_name: vault-service-worker
    ports: ["8038:8038"]
    environment:
      VAULT_DB_PATH: /data/vault.db
    volumes:
      - vault-service-data:/data
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8038/health', timeout=5).read()\""]
      <<: *healthcheck-default

  # ── OpenBao — Secrets Management (MPL 2.0, Vault-compatible) ───────────
  # Replaces: HashiCorp Vault (BSL), Cloudflare environment variables / CF Secrets
  # API-compatible with hvac Python client used by vault-service/openbao_client.py
  openbao:
    image: openbao/openbao:latest
    container_name: trancendos-openbao
    environment:
      BAO_DEV_ROOT_TOKEN_ID: "${OPENBAO_ROOT_TOKEN:?required}"
      BAO_DEV_LISTEN_ADDRESS: "0.0.0.0:8200"
    ports:
      - "8200:8200"
    volumes:
      - openbao-data:/openbao/data
    networks:
      - tranc3-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8200/v1/sys/health"]
      <<: *healthcheck-default

  # ── KrakenD — API Gateway (Apache 2.0, replaces CF trancendos-api-gateway) ──
  # Stateless, no DB; routes /api/v1/* to backend workers with JWT + rate limiting.
  # KrakenD API gateway — main API on :8080 (internal), :8099 (host), metrics on :8090
  krakend:
    image: devopsfaith/krakend:latest
    container_name: trancendos-krakend
    environment:
      - JWT_SECRET=${JWT_SECRET}
    ports:
      - "8099:8080"
      - "8090:8090"
    volumes:
      - ./config/krakend/krakend.json:/etc/krakend/krakend.json:ro
    networks:
      - tranc3-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8080/__health"]
      <<: *healthcheck-default

  # ── Blender Worker — Headless 3D Rendering for TranceFlow (Port 8050) ───
  blender-worker:
    <<: *worker-common
    build:
      context: ./workers/blender-worker
      dockerfile: Dockerfile
    container_name: trancendos-blender-worker
    ports:
      - "8050:8050"
    volumes:
      - blender-renders:/app/renders
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8050/health')"]
      <<: *healthcheck-default

  # ── TripoSR Worker — Image-to-3D for Sashas Photo Studio (Port 8051) ──
  triposr-worker:
    <<: *worker-common
    build:
      context: ./workers/triposr-worker
      dockerfile: Dockerfile
    container_name: trancendos-triposr-worker
    ports:
      - "8051:8051"
    volumes:
      - triposr-models:/app/models
      - triposr-outputs:/app/outputs
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8051/health')"]
      <<: *healthcheck-default

  # ── FFmpeg Worker — TateKing Video Processing (Port 8052) ──────────────
  # Zero-cost self-hosted video transcoding, thumbnail extraction, compression.
  # Provides non-blocking job queue via asyncio subprocesses.
  ffmpeg-worker:
    build:
      context: ./workers/ffmpeg-worker
    container_name: trancendos-ffmpeg-worker
    ports:
      - "8052:8052"
    volumes:
      - ffmpeg-workdir:/app/workdir
    networks:
      - tranc3-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8052/health"]
      <<: *healthcheck-default

  # ── Langfuse — AI Observability (Port 3002) ──────────────────────────
  # MIT-licensed self-hosted LLM observability: traces, generations, evals.
  # Unlimited spans self-hosted; integrates with The Observatory.
  # SDK env vars: LANGFUSE_PUBLIC_KEY, LANGFUSE_SECRET_KEY, LANGFUSE_HOST=http://langfuse:3000
  langfuse:
    image: langfuse/langfuse:latest
    container_name: trancendos-langfuse
    ports:
      - "3002:3000"
    environment:
      DATABASE_URL: "postgresql://langfuse:langfuse@langfuse-db:5432/langfuse"
      NEXTAUTH_URL: "http://localhost:3002"
      NEXTAUTH_SECRET: "${LANGFUSE_SECRET:?required}"
      SALT: "${LANGFUSE_SALT:?required}"
      TELEMETRY_ENABLED: "false"
    depends_on:
      - langfuse-db
    networks:
      - tranc3-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3000/api/public/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  langfuse-db:
    image: postgres:16-alpine
    container_name: trancendos-langfuse-db
    environment:
      POSTGRES_USER: langfuse
      POSTGRES_PASSWORD: langfuse
      POSTGRES_DB: langfuse
    volumes:
      - langfuse-db-data:/var/lib/postgresql/data
    networks:
      - tranc3-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U langfuse -d langfuse"]
      interval: 15s
      timeout: 5s
      retries: 5

  # ── Infinity Admin — Entity & Config Management (Port 8044) ──────────────
  infinity-admin:
    <<: *worker-common
    build:
      context: .
      dockerfile: workers/infinity-admin-service/Dockerfile
    container_name: infinity-admin-worker
    ports:
      - "8044:8044"
    volumes:
      - admin-data:/data
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8044/health', timeout=5).read()\""]
      <<: *healthcheck-default

  # ── Infinity Shards — Pluggable Entity Power-Ups (Port 8045) ─────────────
  infinity-shards:
    <<: *worker-common
    build:
      context: .
      dockerfile: workers/infinity-shards-service/Dockerfile
    container_name: infinity-shards-worker
    ports:
      - "8045:8045"
    environment:
      INFINITY_SHARDS_DB_PATH: /app/data/infinity_shards.db
      AUTH_SERVICE_URL: http://infinity-auth-worker:8005
    volumes:
      - shards-data:/app/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8045/health"]
      <<: *healthcheck-default

  # ── Infinity Bridge — Human Traffic Transfer Hub (Port 8070) ──────────────
  infinity-bridge:
    <<: *worker-common
    build:
      context: .
      dockerfile: workers/infinity-bridge-service/Dockerfile
    container_name: infinity-bridge-worker
    ports:
      - "8070:8070"
    environment:
      INFINITY_BRIDGE_PORT: "8070"
      INTERNAL_SECRET: ${INTERNAL_SECRET:?required}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8070/health"]
      <<: *healthcheck-default

  # ── CranBania — The Town Hall (Kanban + ITSM + Agile, Port 8071) ─────────────
  # Entity: The Town Hall | Lead AI: Tristuran | Source: github.com/Trancendos/CranBania
  # Governance hub: PRINCE2, ITIL, Agile/Kanban, ITSM, visual boards, workshops
  # MCP server (40+ tools) auto-registered with The Spark on startup
  # Magna Carta alignment: change cards enforce governance gate (MC-RULE-007)
  cranbania:
    <<: *worker-common
    build:
      context: workers/cranbania
      dockerfile: Dockerfile
    container_name: cranbania-worker
    ports:
      - "8071:8071"
    environment:
      PORT: "8071"
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
      CRANBANIA_DATA_DIR: /app/data
      CRANBANIA_API_KEY: ${CRANBANIA_API_KEY:-}
      CRANBANIA_CRON_SECRET: ${CRANBANIA_CRON_SECRET:-}
      CRANBANIA_FORGEJO_URL: ${CRANBANIA_FORGEJO_URL:-http://forgejo:3000}
      CRANBANIA_WEBHOOK_SECRET: ${CRANBANIA_WEBHOOK_SECRET:-}
      CRANBANIA_OBSERVATORY_URL: http://monitoring-worker:8007
      MAGNA_CARTA_ENABLED: ${MAGNA_CARTA_ENABLED:-false}
    volumes:
      - cranbania-data:/app/data
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:8071/api/board"]
      <<: *healthcheck-default
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.cranbania.rule=Host(`trancendos.com`) && PathPrefix(`/townhall`)"
      - "traefik.http.routers.cranbania.entrypoints=websecure"
      - "traefik.http.routers.cranbania.tls=true"
      - "traefik.http.routers.cranbania.tls.certresolver=letsencrypt"
      - "traefik.http.routers.cranbania.middlewares=strip-townhall@docker,ratelimit-cranbania@docker"
      - "traefik.http.middlewares.strip-townhall.stripprefix.prefixes=/townhall"
      - "traefik.http.middlewares.ratelimit-cranbania.ratelimit.average=100"
      - "traefik.http.middlewares.ratelimit-cranbania.ratelimit.burst=50"
      - "traefik.http.services.cranbania.loadbalancer.server.port=8071"

  # ── Qdrant — High-performance vector database (Apache 2.0) ─────────────
  # Free: self-hosted with unlimited vectors
  # Replaces Pinecone/paid vector services for The Spark RAG-MCP + knowledge search
  qdrant:
    image: qdrant/qdrant:latest
    container_name: trancendos-qdrant
    restart: unless-stopped
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - qdrant-data:/qdrant/storage
    environment:
      QDRANT__SERVICE__HTTP_PORT: "6333"
      QDRANT__SERVICE__GRPC_PORT: "6334"
      QDRANT__LOG_LEVEL: INFO
    networks:
      - tranc3-net
    deploy:
      resources:
        limits:
          memory: 512m
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:6333/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ── Penpot — Open-source design platform (MPL 2.0), Figma alternative ──
  # Fabulousa: Baron Von Hilton — Styling, UX, UI & design center
  penpot-frontend:
    image: penpotapp/frontend:latest
    container_name: trancendos-penpot-frontend
    restart: unless-stopped
    ports:
      - "9001:80"
    environment:
      PENPOT_FLAGS: "enable-registration enable-login disable-demo-users"
    depends_on:
      - penpot-backend
      - penpot-exporter
    networks:
      - tranc3-net
    labels:
      traefik.enable: "true"
      traefik.http.routers.penpot.rule: "Host(`design.trancendos.com`)"
      traefik.http.routers.penpot.entrypoints: "websecure"
      traefik.http.routers.penpot.tls.certresolver: "letsencrypt"

  penpot-backend:
    image: penpotapp/backend:latest
    container_name: trancendos-penpot-backend
    restart: unless-stopped
    environment:
      PENPOT_FLAGS: "enable-registration enable-login disable-demo-users disable-secure-session-cookies"
      PENPOT_SECRET_KEY: "${PENPOT_SECRET_KEY:?required}"
      PENPOT_DATABASE_URI: "postgresql://penpot:penpot@penpot-db/penpot"
      PENPOT_REDIS_URI: "redis://valkey:6379/2"
      PENPOT_STORAGE_BACKEND: "fs"
      PENPOT_STORAGE_FS_DIRECTORY: "/opt/data/assets"
      PENPOT_ASSETS_STORAGE_BACKEND: "assets-fs"
      PENPOT_STORAGE_ASSETS_FS_DIRECTORY: "/opt/data/assets"
    volumes:
      - penpot-data:/opt/data
    depends_on:
      - penpot-db
    networks:
      - tranc3-net

  penpot-exporter:
    image: penpotapp/exporter:latest
    container_name: trancendos-penpot-exporter
    restart: unless-stopped
    environment:
      PENPOT_PUBLIC_URI: "http://penpot-frontend"
    networks:
      - tranc3-net

  penpot-db:
    image: postgres:15-alpine
    container_name: trancendos-penpot-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: penpot
      POSTGRES_USER: penpot
      POSTGRES_PASSWORD: penpot
    volumes:
      - penpot-db-data:/var/lib/postgresql/data
    networks:
      - tranc3-net

  # ── OWASP Dependency-Track — SBOM vulnerability monitoring (Apache 2.0) ─
  # Consumes CycloneDX SBOMs generated by syft in CI, tracks CVEs continuously
  dependency-track-apiserver:
    image: dependencytrack/apiserver:latest
    container_name: trancendos-dtrack-api
    restart: unless-stopped
    environment:
      ALPINE_DATABASE_MODE: internal
      ALPINE_METRICS_ENABLED: "true"
    volumes:
      - dtrack-data:/data
    ports:
      - "8083:8080"
    networks:
      - tranc3-net
    deploy:
      resources:
        limits:
          memory: 768m
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8080/api/version"]
      interval: 60s
      timeout: 10s
      retries: 5
      start_period: 60s

  dependency-track-frontend:
    image: dependencytrack/frontend:latest
    container_name: trancendos-dtrack-frontend
    restart: unless-stopped
    environment:
      API_BASE_URL: "http://dependency-track-apiserver:8080"
    ports:
      - "8084:8080"
    depends_on:
      - dependency-track-apiserver
    networks:
      - tranc3-net
    labels:
      traefik.enable: "true"
      traefik.http.routers.dtrack.rule: "Host(`sbom.trancendos.com`)"
      traefik.http.routers.dtrack.entrypoints: "websecure"
      traefik.http.routers.dtrack.tls.certresolver: "letsencrypt"

  # Falco — Runtime security monitoring (Apache 2.0, eBPF-based)
  falco:
    image: falcosecurity/falco-no-driver:latest
    container_name: trancendos-falco
    restart: unless-stopped
    privileged: true
    pid: host
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /proc:/host/proc:ro
      - /etc:/host/etc:ro
      - ./config/falco:/etc/falco:ro
    environment:
      HOST_ROOT: /host
      FALCO_HOSTNAME: trancendos-falco
    networks:
      - tranc3-net
    deploy:
      resources:
        limits:
          memory: 256m

  # ═══════════════════════════════════════════════════════════════════════
  # CI/CD LAYER — Woodpecker CI (alongside Forgejo / The Workshop)
  # ═══════════════════════════════════════════════════════════════════════

  # ── Woodpecker Server — CI orchestrator (Port 8100) ──────────────────
  # Apache 2.0 licensed self-hosted CI. Integrates with Forgejo (The Workshop).
  # Dashboard: https://ci.trancendos.com  (Traefik routes Host ci.trancendos.com)
  # Required env: WOODPECKER_FORGEJO_CLIENT, WOODPECKER_FORGEJO_SECRET,
  #               WOODPECKER_SECRET, WOODPECKER_AGENT_TOKEN
  woodpecker-server:
    image: woodpeckerci/woodpecker-server:latest
    container_name: trancendos-woodpecker-server
    restart: unless-stopped
    ports:
      - "8100:8000"
    environment:
      WOODPECKER_OPEN: "true"
      WOODPECKER_FORGEJO: "true"
      WOODPECKER_FORGEJO_URL: "${FORGEJO_URL:-http://forgejo:3000}"
      WOODPECKER_FORGEJO_CLIENT: "${WOODPECKER_FORGEJO_CLIENT}"
      WOODPECKER_FORGEJO_SECRET: "${WOODPECKER_FORGEJO_SECRET}"
      WOODPECKER_SECRET: "${WOODPECKER_SECRET:?required}"
      WOODPECKER_DATABASE_DATASOURCE: "/var/lib/woodpecker/woodpecker.db"
      WOODPECKER_DATABASE_DRIVER: "sqlite3"
    volumes:
      - woodpecker-data:/var/lib/woodpecker
    networks:
      - tranc3-net
    labels:
      traefik.enable: "true"
      traefik.http.routers.woodpecker.rule: "Host(`ci.trancendos.com`)"
      traefik.http.routers.woodpecker.entrypoints: "websecure"
      traefik.http.routers.woodpecker.tls.certresolver: "letsencrypt"

  # ── Woodpecker Agent — CI runner ─────────────────────────────────────
  # Executes pipeline steps inside Docker containers (zero-cost, local).
  woodpecker-agent:
    image: woodpeckerci/woodpecker-agent:latest
    container_name: trancendos-woodpecker-agent
    restart: unless-stopped
    depends_on:
      - woodpecker-server
    environment:
      WOODPECKER_SERVER: "woodpecker-server:9000"
      WOODPECKER_TOKEN: "${WOODPECKER_AGENT_TOKEN:?required}"
      WOODPECKER_MAX_WORKFLOWS: "4"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - tranc3-net

  # ─────────────────────────────────────────────────────────────────────────
  # Cal.com — ChronosSphere / ArcStream (self-hosted scheduling, MIT)
  # UI: http://localhost:3010  Env-set: http://scheduling.trancendos.com
  # ─────────────────────────────────────────────────────────────────────────
  calcom-db:
    image: postgres:15-alpine
    container_name: trancendos-calcom-db
    environment:
      POSTGRES_USER: calcom
      POSTGRES_PASSWORD: "${CALCOM_DB_PASSWORD:?required}"
      POSTGRES_DB: calcom
    volumes:
      - calcom-db-data:/var/lib/postgresql/data
    networks:
      - tranc3-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U calcom -d calcom"]
      interval: 30s
      timeout: 10s
      retries: 3

  calcom:
    image: calcom/cal.com:latest
    container_name: trancendos-calcom
    ports:
      - "3010:3000"
    environment:
      DATABASE_URL: "postgresql://calcom:${CALCOM_DB_PASSWORD:?required}@calcom-db:5432/calcom"
      NEXTAUTH_SECRET: "${CALCOM_NEXTAUTH_SECRET:?required}"
      CALENDSO_ENCRYPTION_KEY: "${CALCOM_ENCRYPTION_KEY:?required}"
      NEXT_PUBLIC_WEBAPP_URL: "${CALCOM_URL:-http://localhost:3010}"
      EMAIL_FROM: "${CALCOM_EMAIL_FROM:-noreply@trancendos.com}"
      NEXT_PUBLIC_LICENSE_CONSENT: "agree"
    depends_on:
      calcom-db:
        condition: service_healthy
    volumes:
      - calcom-data:/app/.next
    networks:
      - tranc3-net
    restart: unless-stopped
    labels:
      traefik.enable: "true"
      traefik.http.routers.calcom.rule: "Host(`scheduling.trancendos.com`)"
      traefik.http.routers.calcom.entrypoints: "websecure"
      traefik.http.routers.calcom.tls.certresolver: "letsencrypt"
      traefik.http.services.calcom.loadbalancer.server.port: "3000"

  # ─────────────────────────────────────────────────────────────────────────
  # Outline — The Library / Zimik (self-hosted wiki, BSL)
  # UI: http://localhost:3011  Env-set: http://wiki.trancendos.com
  # ─────────────────────────────────────────────────────────────────────────
  outline-db:
    image: postgres:15-alpine
    container_name: trancendos-outline-db
    environment:
      POSTGRES_USER: outline
      POSTGRES_PASSWORD: "${OUTLINE_DB_PASSWORD:?required}"
      POSTGRES_DB: outline
    volumes:
      - outline-db-data:/var/lib/postgresql/data
    networks:
      - tranc3-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U outline -d outline"]
      interval: 30s
      timeout: 10s
      retries: 3

  outline-redis:
    image: valkey/valkey:8-alpine
    container_name: trancendos-outline-redis
    command: ["valkey-server", "--appendonly", "yes"]
    volumes:
      - outline-redis-data:/data
    networks:
      - tranc3-net
    restart: unless-stopped

  outline:
    image: outlinewiki/outline:latest
    container_name: trancendos-outline
    ports:
      - "3011:3000"
    environment:
      DATABASE_URL: "postgres://outline:${OUTLINE_DB_PASSWORD:?required}@outline-db:5432/outline"
      REDIS_URL: "redis://outline-redis:6379"
      SECRET_KEY: "${OUTLINE_SECRET_KEY:?required}"
      UTILS_SECRET: "${OUTLINE_UTILS_SECRET:?required}"
      URL: "${OUTLINE_URL:-http://localhost:3011}"
      PORT: "3000"
      FILE_STORAGE: "local"
      FILE_STORAGE_LOCAL_ROOT_DIR: "/var/lib/outline/data"
      PGSSLMODE: "disable"
    depends_on:
      outline-db:
        condition: service_healthy
      outline-redis:
        condition: service_started
    volumes:
      - outline-data:/var/lib/outline/data
    networks:
      - tranc3-net
    restart: unless-stopped
    labels:
      traefik.enable: "true"
      traefik.http.routers.outline.rule: "Host(`wiki.trancendos.com`)"
      traefik.http.routers.outline.entrypoints: "websecure"
      traefik.http.routers.outline.tls.certresolver: "letsencrypt"
      traefik.http.services.outline.loadbalancer.server.port: "3000"

  # ─────────────────────────────────────────────────────────────────────────
  # SigNoz — The Observatory enhanced (OpenTelemetry-native APM, Apache 2.0)
  # UI: http://localhost:3301  Backend: ClickHouse + query-service
  # ─────────────────────────────────────────────────────────────────────────
  signoz-clickhouse:
    image: clickhouse/clickhouse-server:24.1.2-alpine
    container_name: trancendos-signoz-clickhouse
    environment:
      CLICKHOUSE_DB: signoz_traces
      CLICKHOUSE_USER: signoz
      CLICKHOUSE_PASSWORD: "${SIGNOZ_CLICKHOUSE_PASSWORD:?required}"
      CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT: "1"
    volumes:
      - signoz-clickhouse-data:/var/lib/clickhouse
      - signoz-clickhouse-logs:/var/log/clickhouse-server
    networks:
      - tranc3-net
    restart: unless-stopped
    ulimits:
      nofile:
        soft: 262144
        hard: 262144
    healthcheck:
      test: ["CMD", "clickhouse-client", "--query", "SELECT 1"]
      interval: 30s
      timeout: 10s
      retries: 5

  signoz-query-service:
    image: signoz/query-service:latest
    container_name: trancendos-signoz-query
    command: ["-config=/root/config/prometheus.yml"]
    environment:
      ClickHouseUrl: "tcp://signoz:${SIGNOZ_CLICKHOUSE_PASSWORD:?required}@signoz-clickhouse:9000/signoz_traces"
      STORAGE: clickhouse
      GODEBUG: netdns=go
      TELEMETRY_ENABLED: "false"
      DEPLOYMENT_TYPE: "docker-standalone"
    volumes:
      - signoz-query-data:/root/data
      - ./monitoring/prometheus.yml:/root/config/prometheus.yml:ro
    depends_on:
      signoz-clickhouse:
        condition: service_healthy
    networks:
      - tranc3-net
    restart: unless-stopped

  signoz-frontend:
    image: signoz/frontend:latest
    container_name: trancendos-signoz-frontend
    ports:
      - "3301:3301"
    environment:
      FRONTEND_API_ENDPOINT: "http://signoz-query-service:8080"
    depends_on:
      - signoz-query-service
    networks:
      - tranc3-net
    restart: unless-stopped
    labels:
      traefik.enable: "true"
      traefik.http.routers.signoz.rule: "Host(`apm.trancendos.com`)"
      traefik.http.routers.signoz.entrypoints: "websecure"
      traefik.http.routers.signoz.tls.certresolver: "letsencrypt"
      traefik.http.services.signoz.loadbalancer.server.port: "3301"

  # SigNoz OpenTelemetry Collector (receives OTLP from all workers)
  signoz-otel-collector:
    image: signoz/signoz-otel-collector:latest
    container_name: trancendos-signoz-otel
    command: ["--config=/etc/otel-collector-config.yaml"]
    environment:
      OTEL_RESOURCE_ATTRIBUTES: "host.name=trancendos-production,os.type=linux"
      LOW_CARDINAL_EXCEPTION_GROUPING: "false"
    volumes:
      - ./config/signoz/otel-collector-config.yaml:/etc/otel-collector-config.yaml:ro
    ports:
      - "24317:4317"   # OTLP gRPC (host 24317; avoids conflict with otel-collector on 4317)
      - "24318:4318"   # OTLP HTTP (host 24318; avoids conflict with otel-collector on 4318)
    depends_on:
      - signoz-clickhouse
    networks:
      - tranc3-net
    restart: unless-stopped

  # ══════════════════════════════════════════════════════════════════════════
  # ADAPTIVE AI LAYER — LiteLLM Proxy (Apache 2.0, zero-cost)
  # Unified OpenAI-compatible endpoint over x8 free provider backends.
  # Port 4000. Route: ai.trancendos.com/litellm
  # Providers: ollama → groq → cerebras → sambanova → gemini → openrouter
  #            → mistral → huggingface (x8 rotation with hard stops)
  # ══════════════════════════════════════════════════════════════════════════
  litellm:
    image: ghcr.io/berriai/litellm:main-latest
    container_name: trancendos-litellm
    command: ["--config", "/app/config.yaml", "--port", "4000", "--num_workers", "4"]
    volumes:
      - ./config/litellm/config.yaml:/app/config.yaml:ro
      - litellm-data:/app/data
    ports:
      - "4000:4000"
    environment:
      LITELLM_MASTER_KEY: "${LITELLM_MASTER_KEY:?required}"
      LITELLM_SALT_KEY: "${LITELLM_SALT_KEY:?required}"
      DATABASE_URL: "sqlite:////app/data/litellm.db"
      STORE_MODEL_IN_DB: "True"
      # Provider API keys (all free tiers)
      GROQ_API_KEY: "${GROQ_API_KEY:-}"
      CEREBRAS_API_KEY: "${CEREBRAS_API_KEY:-}"
      SAMBANOVA_API_KEY: "${SAMBANOVA_API_KEY:-}"
      GEMINI_API_KEY: "${GEMINI_API_KEY:-}"
      OPENROUTER_API_KEY: "${OPENROUTER_API_KEY:-}"
      MISTRAL_API_KEY: "${MISTRAL_API_KEY:-}"
      HUGGINGFACE_API_KEY: "${HUGGINGFACE_API_KEY:-}"
      OLLAMA_URL: "http://ollama:11434"
      # Langfuse observability integration
      LANGFUSE_PUBLIC_KEY: "${LANGFUSE_PUBLIC_KEY:-}"
      LANGFUSE_SECRET_KEY: "${LANGFUSE_SECRET_KEY:-}"
      LANGFUSE_HOST: "http://langfuse:3000"
    depends_on:
      - langfuse
    networks:
      - tranc3-net
    restart: unless-stopped
    labels:
      traefik.enable: "true"
      traefik.http.routers.litellm.rule: "Host(`ai.trancendos.com`)"
      traefik.http.routers.litellm.entrypoints: "websecure"
      traefik.http.routers.litellm.tls.certresolver: "letsencrypt"
      traefik.http.services.litellm.loadbalancer.server.port: "4000"

  # Ollama — local self-hosted LLM inference (zero-cost, tier 0)
  ollama:
    image: ollama/ollama:latest
    container_name: trancendos-ollama
    volumes:
      - ollama-data:/root/.ollama
    ports:
      - "11434:11434"
    environment:
      OLLAMA_ORIGINS: "*"
      OLLAMA_HOST: "0.0.0.0"
    networks:
      - tranc3-net
    restart: unless-stopped
    # GPU support: add 'deploy: resources: reservations: devices: [{driver: nvidia, ...}]'

  # ══════════════════════════════════════════════════════════════════════════
  # SEARCH — Meilisearch (MIT, zero-cost, self-hosted)
  # Full-text search for all platform content. Port 7700.
  # Route: search.trancendos.com
  # Used by: The Library, DocUtari, API Marketplace, The Lab
  # ══════════════════════════════════════════════════════════════════════════
  meilisearch:
    image: getmeili/meilisearch:latest
    container_name: trancendos-meilisearch
    volumes:
      - meilisearch-data:/meili_data
    ports:
      - "7700:7700"
    environment:
      MEILI_MASTER_KEY: "${MEILI_MASTER_KEY:?required}"
      MEILI_ENV: "production"
      MEILI_DB_PATH: "/meili_data"
      MEILI_LOG_LEVEL: "INFO"
    networks:
      - tranc3-net
    restart: unless-stopped
    labels:
      traefik.enable: "true"
      traefik.http.routers.meilisearch.rule: "Host(`search.trancendos.com`)"
      traefik.http.routers.meilisearch.entrypoints: "websecure"
      traefik.http.routers.meilisearch.tls.certresolver: "letsencrypt"
      traefik.http.services.meilisearch.loadbalancer.server.port: "7700"

  # ══════════════════════════════════════════════════════════════════════════
  # OBJECT STORAGE — MinIO (AGPL-3.0, zero-cost, S3-compatible)
  # Replaces Cloudflare R2 / IPFS for blob storage. Port 9000 (API), 9001 (console).
  # Route: s3.trancendos.com (API), s3-console.trancendos.com (console)
  # ══════════════════════════════════════════════════════════════════════════
  minio:
    image: minio/minio:latest
    container_name: trancendos-minio
    command: server /data --console-address ":9001"
    volumes:
      - minio-data:/data
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: "${MINIO_ROOT_USER:-tranc3admin}"
      MINIO_ROOT_PASSWORD: "${MINIO_ROOT_PASSWORD:?required}"
      MINIO_BROWSER_REDIRECT_URL: "https://s3-console.trancendos.com"
    networks:
      - tranc3-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 10s
      retries: 3
    labels:
      traefik.enable: "true"
      # S3 API
      traefik.http.routers.minio-api.rule: "Host(`s3.trancendos.com`)"
      traefik.http.routers.minio-api.entrypoints: "websecure"
      traefik.http.routers.minio-api.tls.certresolver: "letsencrypt"
      traefik.http.routers.minio-api.service: "minio-api-svc"
      traefik.http.services.minio-api-svc.loadbalancer.server.port: "9000"
      # Console
      traefik.http.routers.minio-console.rule: "Host(`s3-console.trancendos.com`)"
      traefik.http.routers.minio-console.entrypoints: "websecure"
      traefik.http.routers.minio-console.tls.certresolver: "letsencrypt"
      traefik.http.routers.minio-console.service: "minio-console-svc"
      traefik.http.services.minio-console-svc.loadbalancer.server.port: "9001"

  # ══════════════════════════════════════════════════════════════════════════
  # IDENTITY & SSO — Authentik (MIT, zero-cost)
  # Replaces Keycloak (heavier) — OAuth2/OIDC/SAML SSO for all services.
  # Complements workers/infinity-auth/ (which handles Trancendos-native auth).
  # Authentik = external SSO bridge (Google, GitHub, enterprise SAML).
  # Port 9300 (HTTP), 9443 (HTTPS). Route: sso.trancendos.com
  # ══════════════════════════════════════════════════════════════════════════
  authentik-server:
    image: ghcr.io/goauthentik/server:latest
    container_name: trancendos-authentik-server
    command: server
    volumes:
      - authentik-media:/media
      - authentik-templates:/templates
    ports:
      - "9300:9000"
      - "9443:9443"
    environment:
      AUTHENTIK_REDIS__HOST: "authentik-redis"
      AUTHENTIK_POSTGRESQL__HOST: "authentik-db"
      AUTHENTIK_POSTGRESQL__USER: "authentik"
      AUTHENTIK_POSTGRESQL__PASSWORD: "${AUTHENTIK_DB_PASSWORD:?required}"
      AUTHENTIK_POSTGRESQL__NAME: "authentik"
      AUTHENTIK_SECRET_KEY: "${AUTHENTIK_SECRET_KEY:?required}"
      AUTHENTIK_ERROR_REPORTING__ENABLED: "false"
      AUTHENTIK_BOOTSTRAP_EMAIL: "${ADMIN_EMAIL:?required}"
      AUTHENTIK_BOOTSTRAP_PASSWORD: "${AUTHENTIK_ADMIN_PASSWORD:?required}"
      AUTHENTIK_BOOTSTRAP_TOKEN: "${AUTHENTIK_BOOTSTRAP_TOKEN:-}"
    depends_on:
      - authentik-db
      - authentik-redis
    networks:
      - tranc3-net
    restart: unless-stopped
    labels:
      traefik.enable: "true"
      traefik.http.routers.authentik.rule: "Host(`sso.trancendos.com`)"
      traefik.http.routers.authentik.entrypoints: "websecure"
      traefik.http.routers.authentik.tls.certresolver: "letsencrypt"
      traefik.http.services.authentik.loadbalancer.server.port: "9000"

  authentik-worker:
    image: ghcr.io/goauthentik/server:latest
    container_name: trancendos-authentik-worker
    command: worker
    environment:
      AUTHENTIK_REDIS__HOST: "authentik-redis"
      AUTHENTIK_POSTGRESQL__HOST: "authentik-db"
      AUTHENTIK_POSTGRESQL__USER: "authentik"
      AUTHENTIK_POSTGRESQL__PASSWORD: "${AUTHENTIK_DB_PASSWORD:?required}"
      AUTHENTIK_POSTGRESQL__NAME: "authentik"
      AUTHENTIK_SECRET_KEY: "${AUTHENTIK_SECRET_KEY:?required}"
      AUTHENTIK_ERROR_REPORTING__ENABLED: "false"
    volumes:
      - authentik-media:/media
      - authentik-certs:/certs
    depends_on:
      - authentik-db
      - authentik-redis
    networks:
      - tranc3-net
    restart: unless-stopped

  authentik-db:
    image: postgres:15-alpine
    container_name: trancendos-authentik-db
    volumes:
      - authentik-db-data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: "authentik"
      POSTGRES_PASSWORD: "${AUTHENTIK_DB_PASSWORD:?required}"
      POSTGRES_DB: "authentik"
    networks:
      - tranc3-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U authentik -d authentik"]
      interval: 30s
      timeout: 5s
      retries: 5

  authentik-redis:
    image: redis:7-alpine
    container_name: trancendos-authentik-redis
    volumes:
      - authentik-redis-data:/data
    networks:
      - tranc3-net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]

  # ══════════════════════════════════════════════════════════════════════════
  # CONTAINER LOGS UI — Dozzle (MIT, zero-cost)
  # Real-time Docker log viewer for all containers. Port 8888.
  # Route: logs.trancendos.com
  # ══════════════════════════════════════════════════════════════════════════
  dozzle:
    image: amir20/dozzle:latest
    container_name: trancendos-dozzle
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    ports:
      - "8888:8080"
    environment:
      DOZZLE_AUTH_PROVIDER: "simple"
      DOZZLE_ENABLE_ACTIONS: "true"
      DOZZLE_LEVEL: "info"
    networks:
      - tranc3-net
    restart: unless-stopped
    labels:
      traefik.enable: "true"
      traefik.http.routers.dozzle.rule: "Host(`logs.trancendos.com`)"
      traefik.http.routers.dozzle.entrypoints: "websecure"
      traefik.http.routers.dozzle.tls.certresolver: "letsencrypt"
      traefik.http.services.dozzle.loadbalancer.server.port: "8080"

  # ══════════════════════════════════════════════════════════════════════════
  # COLLABORATIVE WHITEBOARD — Excalidraw (MIT, zero-cost)
  # Infinite canvas for design brainstorming, architecture diagrams.
  # Complements Penpot (Fabulousa) for freeform collaboration.
  # Port 3800. Route: draw.trancendos.com
  # ══════════════════════════════════════════════════════════════════════════
  excalidraw:
    image: excalidraw/excalidraw:latest
    container_name: trancendos-excalidraw
    ports:
      - "3800:80"
    networks:
      - tranc3-net
    restart: unless-stopped
    labels:
      traefik.enable: "true"
      traefik.http.routers.excalidraw.rule: "Host(`draw.trancendos.com`)"
      traefik.http.routers.excalidraw.entrypoints: "websecure"
      traefik.http.routers.excalidraw.tls.certresolver: "letsencrypt"
      traefik.http.services.excalidraw.loadbalancer.server.port: "80"

# ═══════════════════════════════════════════════════════════════════════════
# Item 8 — n8n: Workflow node upgrade for The Digital Grid
# ═══════════════════════════════════════════════════════════════════════════
  n8n:
    image: n8nio/n8n:latest
    container_name: trancendos-n8n
    restart: unless-stopped
    environment:
      - N8N_HOST=0.0.0.0
      - N8N_PORT=5678
      - N8N_PROTOCOL=http
      - NODE_ENV=production
      - WEBHOOK_URL=http://n8n:5678/
      - GENERIC_TIMEZONE=UTC
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=${N8N_USER:-admin}
      - N8N_BASIC_AUTH_PASSWORD=${N8N_PASSWORD:?n8n password required}
      - DB_TYPE=sqlite
      - DB_SQLITE_DATABASE=/home/node/.n8n/database.sqlite
      - N8N_ENCRYPTION_KEY=${N8N_ENCRYPTION_KEY:?required}
    volumes:
      - n8n-data:/home/node/.n8n
    ports:
      - "5678:5678"
    networks:
      - tranc3-net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.n8n.rule=Host(`trancendos.com`) && PathPrefix(`/n8n`)"
      - "traefik.http.services.n8n.loadbalancer.server.port=5678"

# ═══════════════════════════════════════════════════════════════════════════
# Item 7 — Wazuh: SIEM/EDR for Cryptex (single-node, free OSS)
# Requires: vm.max_map_count=262144 on host (sysctl -w vm.max_map_count=262144)
# ═══════════════════════════════════════════════════════════════════════════
  wazuh-manager:
    image: wazuh/wazuh-manager:4.8.0
    container_name: trancendos-wazuh-manager
    restart: unless-stopped
    environment:
      - WAZUH_API_USER=${WAZUH_API_USER:-wazuh}
      - WAZUH_API_PASSWORD=${WAZUH_API_PASSWORD:?required}
      - WAZUH_MANAGER_IP=0.0.0.0
    volumes:
      - wazuh-manager-data:/var/ossec/data
      - wazuh-manager-logs:/var/ossec/logs
      - wazuh-manager-etc:/var/ossec/etc
    ports:
      - "1514:1514/udp"
      - "1515:1515"
      - "55000:55000"
    networks:
      - tranc3-net

  wazuh-dashboard:
    image: wazuh/wazuh-dashboard:4.8.0
    container_name: trancendos-wazuh-dashboard
    restart: unless-stopped
    depends_on:
      - wazuh-manager
    environment:
      - WAZUH_API_URL=https://wazuh-manager
      - WAZUH_API_PORT=55000
      - WAZUH_API_USER=${WAZUH_API_USER:-wazuh}
      - WAZUH_API_PASSWORD=${WAZUH_API_PASSWORD}
      - OPENSEARCH_HOSTS=https://wazuh-indexer:9200
      - OPENSEARCH_USERNAME=kibanaserver
      - OPENSEARCH_PASSWORD=${WAZUH_KIBANA_PASSWORD:-kibanaserver}
    ports:
      - "5601:5601"
    networks:
      - tranc3-net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.wazuh.rule=Host(`trancendos.com`) && PathPrefix(`/wazuh`)"
      - "traefik.http.services.wazuh.loadbalancer.server.port=5601"

  wazuh-indexer:
    image: wazuh/wazuh-indexer:4.8.0
    container_name: trancendos-wazuh-indexer
    restart: unless-stopped
    environment:
      - "OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m"
      - network.host=0.0.0.0
      - node.name=wazuh-indexer
      - cluster.initial_cluster_manager_nodes=wazuh-indexer
      - OPENSEARCH_INITIAL_ADMIN_PASSWORD=${WAZUH_INDEXER_PASSWORD:?required}
    volumes:
      - wazuh-indexer-data:/var/lib/wazuh-indexer
    networks:
      - tranc3-net

# ═══════════════════════════════════════════════════════════════════════════
# Item 5 — Paperless-ngx: Document management for DocUtari
# ═══════════════════════════════════════════════════════════════════════════
  paperless:
    image: ghcr.io/paperless-ngx/paperless-ngx:latest
    container_name: trancendos-paperless
    restart: unless-stopped
    depends_on:
      - paperless-db
      - paperless-redis
    environment:
      - PAPERLESS_REDIS=redis://paperless-redis:6379
      - PAPERLESS_DBHOST=paperless-db
      - PAPERLESS_DBNAME=${PAPERLESS_DB_NAME:-paperless}
      - PAPERLESS_DBUSER=${PAPERLESS_DB_USER:-paperless}
      - PAPERLESS_DBPASS=${PAPERLESS_DB_PASSWORD:?required}
      - PAPERLESS_SECRET_KEY=${PAPERLESS_SECRET_KEY:?required}
      - PAPERLESS_URL=http://trancendos.com/docutari
      - PAPERLESS_ADMIN_USER=${PAPERLESS_ADMIN_USER:-admin}
      - PAPERLESS_ADMIN_PASSWORD=${PAPERLESS_ADMIN_PASSWORD:?required}
      - PAPERLESS_OCR_LANGUAGE=eng
      - PAPERLESS_TIME_ZONE=UTC
    volumes:
      - paperless-data:/usr/src/paperless/data
      - paperless-media:/usr/src/paperless/media
      - paperless-export:/usr/src/paperless/export
      - paperless-consume:/usr/src/paperless/consume
    ports:
      - "8085:8000"
    networks:
      - tranc3-net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.paperless.rule=Host(`trancendos.com`) && PathPrefix(`/docutari`)"
      - "traefik.http.services.paperless.loadbalancer.server.port=8000"

  paperless-db:
    image: postgres:15-alpine
    container_name: trancendos-paperless-db
    restart: unless-stopped
    environment:
      - POSTGRES_DB=${PAPERLESS_DB_NAME:-paperless}
      - POSTGRES_USER=${PAPERLESS_DB_USER:-paperless}
      - POSTGRES_PASSWORD=${PAPERLESS_DB_PASSWORD}
    volumes:
      - paperless-db-data:/var/lib/postgresql/data
    networks:
      - tranc3-net

  paperless-redis:
    image: redis:7-alpine
    container_name: trancendos-paperless-redis
    restart: unless-stopped
    networks:
      - tranc3-net

# ═══════════════════════════════════════════════════════════════════════════
# Item 7 — OpenBAS: Breach & attack simulation for The Ice Box
# ═══════════════════════════════════════════════════════════════════════════
  openbas:
    image: filigran/openbas-platform:latest
    container_name: trancendos-openbas
    restart: unless-stopped
    depends_on:
      - openbas-db
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://openbas-db:5432/openbas
      - SPRING_DATASOURCE_USERNAME=${OPENBAS_DB_USER:-openbas}
      - SPRING_DATASOURCE_PASSWORD=${OPENBAS_DB_PASSWORD:?required}
      - OPENBAS_ADMIN_EMAIL=${OPENBAS_ADMIN_EMAIL:-admin@trancendos.com}
      - OPENBAS_ADMIN_PASSWORD=${OPENBAS_ADMIN_PASSWORD:?required}
      - OPENBAS_ADMIN_TOKEN=${OPENBAS_ADMIN_TOKEN:?required}
      - OPENBAS_BASE_URL=http://trancendos.com/openbas
      - SERVER_PORT=8082
    volumes:
      - openbas-data:/opt/openbas/files
    ports:
      - "8082:8082"
    networks:
      - tranc3-net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.openbas.rule=Host(`trancendos.com`) && PathPrefix(`/openbas`)"
      - "traefik.http.services.openbas.loadbalancer.server.port=8082"

  openbas-db:
    image: postgres:15-alpine
    container_name: trancendos-openbas-db
    restart: unless-stopped
    environment:
      - POSTGRES_DB=openbas
      - POSTGRES_USER=${OPENBAS_DB_USER:-openbas}
      - POSTGRES_PASSWORD=${OPENBAS_DB_PASSWORD}
    volumes:
      - openbas-db-data:/var/lib/postgresql/data
    networks:
      - tranc3-net

# ═══════════════════════════════════════════════════════════════════════════
# Volumes
# ═══════════════════════════════════════════════════════════════════════════
volumes:
  cranbania-data:
    driver: local
  traefik-config:
    driver: local
  traefik-certs:
    driver: local
  vault-data:
    driver: local
  vault-config:
    driver: local
  backend-data:
    driver: local
  backend-models:
    driver: local
  backend-cache:
    driver: local
  backend-logs:
    driver: local
  prometheus-data:
    driver: local
  alertmanager-data:
    driver: local
  grafana-data:
    driver: local
  loki-data:
    driver: local
  ipfs-data:
    driver: local
  void-data:
    driver: local
  vault-service-data:
    driver: local
  admin-data:
    driver: local
  auth-data:
    driver: local
  users-data:
    driver: local
  monitoring-data:
    driver: local
  notifications-data:
    driver: local
  ai-data:
    driver: local
  grid-data:
    driver: local
  products-data:
    driver: local
  orders-data:
    driver: local
  payments-data:
    driver: local
  files-data:
    driver: local
  identity-data:
    driver: local
  nats-data:
    driver: local
  tempo-data:
    driver: local
  victoriametrics-data:
    driver: local
  valkey-data:
    driver: local
  zot-data:
    driver: local
  openbao-data:
    driver: local
  blender-renders:
    driver: local
  triposr-models:
    driver: local
  triposr-outputs:
    driver: local
  ffmpeg-workdir:
    driver: local
  langfuse-db-data:
    driver: local
  woodpecker-data:
    driver: local
  qdrant-data:
    driver: local
  penpot-data:
    driver: local
  penpot-db-data:
    driver: local
  dtrack-data:
    driver: local
  calcom-db-data:
    driver: local
  calcom-data:
    driver: local
  outline-db-data:
    driver: local
  outline-redis-data:
    driver: local
  outline-data:
    driver: local
  signoz-clickhouse-data:
    driver: local
  signoz-clickhouse-logs:
    driver: local
  signoz-query-data:
    driver: local
  litellm-data:
    driver: local
  ollama-data:
    driver: local
  meilisearch-data:
    driver: local
  minio-data:
    driver: local
  authentik-media:
    driver: local
  authentik-templates:
    driver: local
  authentik-certs:
    driver: local
  authentik-db-data:
    driver: local
  authentik-redis-data:
    driver: local
  n8n-data:
    driver: local
  wazuh-manager-data:
    driver: local
  wazuh-manager-logs:
    driver: local
  wazuh-manager-etc:
    driver: local
  wazuh-indexer-data:
    driver: local
  paperless-data:
    driver: local
  paperless-media:
    driver: local
  paperless-export:
    driver: local
  paperless-consume:
    driver: local
  paperless-db-data:
    driver: local
  openbas-data:
    driver: local
  openbas-db-data:
    driver: local

# ═══════════════════════════════════════════════════════════════════════════
# Networks
# ═══════════════════════════════════════════════════════════════════════════
networks:
  tranc3-net:
    driver: bridge
    ipam:
      config:
        - subnet: 172.28.0.0/16
