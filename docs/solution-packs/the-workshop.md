# Solution Pack — The Workshop

> **PID-WRK · AID-WRK-01 · Development (Code) pillar**
>
> Generated by `scripts/build_solution_packs.py`. **DERIVED** sections are read
> from the registers named inline and are verifiable. **SCAFFOLD** sections are
> generated starting points shaped by those facts — drafts to accept or replace,
> not descriptions of what exists.

## 1. What this is — DERIVED

| Field | Value | Source |
|---|---|---|
| Location | The Workshop | `src/entities/platform.py` |
| Product ID | `PID-WRK` | `_assign_ids()` |
| Lead AI | Larry Lowhammer (`AID-WRK-01`) | `src/entities/platform.py` |
| Base model tier | Tranc3 | `get_orchestration_tier()` |
| Job Description | Head of Source Control & CI/CD | `docs/governance/LOCATION-FUNCTIONS.md` |
| Pillar | Development (Code) | `Pillar` enum |
| Reports to Prime(s) | The Dr. (Nikolai O'denhime) | `primes` |
| Status | ✅ In repo | `CLAUDE.md` service table |
| Code path | `deploy/forgejo/` ✅ on disk | filesystem |
| Port | 2222 | compose / `worker_port` |
| Compose service | `forgejo` | `docker-compose.production.yml` |
| Traefik route | `Host(`the-workshop.trancendos.com`)` | compose labels |

**Role.** CI/CD hub — Forgejo self-hosted git + pipelines

## 2. What it does — DERIVED

**Primary function.** Repository Storage (Forgejo)

**Abilities**
- Distributed Forgejo Sync: Highly available repo hosting.
- Disaster Recovery Backup: Instant code regeneration.

**Operating modes**

- *Online:* Active repo hosting; cloud PR management; live merging.
- *Offline:* Local Git tree management; offline commits/branching.

The offline mode is a design constraint, not a footnote: it states what this
Location must still deliver when its upstreams are unreachable, and any
implementation that cannot honour it is incomplete regardless of test coverage.

## 3. Architectural requirements — DERIVED constraints, SCAFFOLD targets

**Hard constraints — these come from the estate, not from preference.**

- Runs in-process under `api.py` (path `deploy/forgejo/`), so it *may*
  import from `src/` — but that also means it shares the backend's failure domain.
- **SQLite over shared state** — each worker owns its own database file (principle 1).
- **In-memory token-bucket rate limiting** — no external KV (principle 2).
- **Zero-cost posture** — no paid dependency may be introduced without funding sign-off.
- **No `stripprefix` on `/the-workshop`** — and that is deliberate: this
  worker serves the prefixed paths itself, so stripping would route `/the-workshop/x`
  to `/x`, which it does not serve. Adding the middleware would break it.

**Non-functional targets — SCAFFOLD, set these against real measurements.**

| Attribute | Starting target | Why this number needs replacing |
|---|---|---|
| Health endpoint | `GET /health` < 200 ms | compose healthcheck already polls it |
| p95 latency | < 500 ms | placeholder — no load profile exists yet |
| Availability | 99% single-node | one chassis; see `deploy/CITADEL_OPERATIONS.md` §9 |
| Data durability | volume-backed + off-box backup | snapshots are not backups |

## 4. Design schematic — DERIVED topology

```mermaid
flowchart LR
    C[Client] --> T[Traefik]
    T -->|/the-workshop| S[The Workshop<br/>2222]
    S --> DB[(SQLite<br/>own file)]
    S -.reports.-> P[The Dr. (Nikolai O'denhime)]
    S --> AA[Branch-Manager]
    S --> AB[Merge-Master]
    AA -.-> CommitBot[Commit-Bot]
    AA -.-> PushBot[Push-Bot]
    AA -.-> PullBot[Pull-Bot]
    AA -.-> CloneBot[Clone-Bot]
    S --> OBS[The Observatory<br/>audit]
```

## 5. Blueprint — SCAFFOLD

| Layer | Component | Note |
|---|---|---|
| Ingress | Traefik → the-workshop | Host(`the-workshop.trancendos.com`) |
| API | FastAPI app | `/health`, `/status`, domain routes |
| Domain | Branch-Manager + Merge-Master | the two Agents below |
| Automation | Commit-Bot, Push-Bot, Pull-Bot, Clone-Bot | the four Bots below |
| Persistence | SQLite | forgejo-data → /data |
| Observability | structured JSON + W3C trace | `src/observability/tracing.py` |

## 6. Storyboard and schema — SCAFFOLD

A first-run journey, derived from the abilities above. Replace with the real
journey once a user has actually walked it.

```
1. Request arrives  →  Traefik strips /the-workshop
2. Branch-Manager — Tracks active code branches, conflicts, and pull requests.
3. Merge-Master — Safely merges code branches, guiding users through conflicts.
4. Bots fire: Commit-Bot, Push-Bot, Pull-Bot, Clone-Bot
5. Result persisted to SQLite; event emitted to The Observatory
6. Response returned with trace_id for correlation
```

**Proposed schema** — shaped by the abilities; not yet implemented.

```sql
CREATE TABLE IF NOT EXISTS the_workshop_records (
    id           INTEGER PRIMARY KEY AUTOINCREMENT,
    record_id    TEXT NOT NULL UNIQUE,
    actor        TEXT,
    payload      TEXT NOT NULL,        -- JSON
    state        TEXT NOT NULL DEFAULT 'new',
    created_at   REAL NOT NULL,
    updated_at   REAL
);
CREATE INDEX IF NOT EXISTS idx_the_workshop_state ON the_workshop_records(state);

CREATE TABLE IF NOT EXISTS the_workshop_audit (
    id           INTEGER PRIMARY KEY AUTOINCREMENT,
    record_id    TEXT NOT NULL,
    action       TEXT NOT NULL,
    actor        TEXT,
    at           REAL NOT NULL
);
```

## 7. Components — DERIVED

**Agents (Tier 4)**

| SID | Agent | Responsibility |
|---|---|---|
| `SID-WRK-01` | Branch-Manager | Tracks active code branches, conflicts, and pull requests. |
| `SID-WRK-02` | Merge-Master | Safely merges code branches, guiding users through conflicts. |

**Bots (Tier 5)**

| NID | Bot | Responsibility |
|---|---|---|
| `NID-WRK-01` | Commit-Bot | Packages file revisions with clear, automated descriptions. |
| `NID-WRK-02` | Push-Bot | Uploads locally saved code changes to the central Forgejo system. |
| `NID-WRK-03` | Pull-Bot | Fetches and updates local folders with the latest repo commits. |
| `NID-WRK-04` | Clone-Bot | Replicates repositories, setting up fresh workspace folders. |

## 8. Design direction — SCAFFOLD

**Foundation.** No OSS foundation is recorded for this Location. Either one
should be chosen and added to CLAUDE.md, or the build-from-scratch decision
should be written down with its reasoning.

**Interface tone.** Development (Code) pillar. Surface the two Agents as the
primary verbs and keep the four Bots as background automation the user never
has to name.

## 9. Templates — SCAFFOLD

**Health response** (matches `to_health_meta()`):

```json
{
  "location": "The Workshop",
  "pillar": "Development (Code)",
  "lead_ai": "Larry Lowhammer",
  "primes": ["The Dr. (Nikolai O'denhime)"],
  "status": "ok"
}
```

**Compose service:**

```yaml
  forgejo:
    build: { context: ., dockerfile: Dockerfile }
    environment: [ PORT=2222 ]
    ports: [ "2222:2222" ]
    labels:
      - "traefik.http.routers.forgejo-subdomain.rule=Host(`the-workshop.trancendos.com`)"
```

## 10. Epics and stories — SCAFFOLD

Sequenced against the readiness gaps below, so the first epic is whatever is
actually missing rather than a generic phase 1.

### Epic 1 — Verify routing end to end

- As a client, requests to `/the-workshop` reach the worker with the prefix stripped.
- As a reviewer, a stripprefix middleware exists and is referenced by the router.

### Epic 2 — Implement the abilities

- As a user, I can exercise: Distributed Forgejo Sync.
- As a user, I can exercise: Disaster Recovery Backup.
- As an auditor, every action emits an Observatory event carrying `PID-WRK`.

### Epic 3 — Prove it works

- As a maintainer, `tests/` covers The Workshop's health, status and each ability.
- As a maintainer, the offline mode above is tested, not assumed.

## 11. Technical debt — DERIVED where evidenced

| Item | Evidence | Impact |
|---|---|---|
| No test files under the code path | filesystem check | Regressions land silently |

## 12. Wireframe — SCAFFOLD

```
┌──────────────────────────────────────────────────────┐
│ The Workshop                                 PID-WRK │
├──────────────────────────────────────────────────────┤
│ Repository Storage (Forgejo)                         │
├──────────────────────────────────────────────────────┤
│  ▸ Branch-Manager           Tracks active code branc│
│  ▸ Merge-Master             Safely merges code branc│
├──────────────────────────────────────────────────────┤
│  bots: Commit-Bot, Push-Bot, Pull-Bot, Clone-Bot    │
├──────────────────────────────────────────────────────┤
│  [ health ]  [ status ]  route /the-workshop        │
└──────────────────────────────────────────────────────┘
```

## 13. Prioritisation — DERIVED

**Criticality 2/10 · Readiness 7/10 → Defer — below median on both axes**

Classified against the estate's own medians (criticality 3, readiness
8 across all 43 Locations), not a fixed threshold — the two axes do not
share a scale, so one absolute cut-off would bucket almost everything together.

They are also kept separate rather than multiplied. Collapsing them would
double-count: status and dependency facts feed both terms, so the product
systematically ranks safe-and-unimportant above important-and-unfinished.

| Axis | Score | Reasons |
|---|---|---|
| Criticality | 2/10 | answers to 1 Prime(s) (+1); externally routed via Traefik (+1) |
| Readiness | 7/10 | status ✅ in CLAUDE.md (+3); code path `deploy/forgejo/` exists on disk (+2); compose service `forgejo` defined (+2) |

## 14. Documentation — DERIVED

- `PLATFORM_ENTITIES.md` — canonical entry for PID-WRK
- `src/entities/platform.py` — `PLATFORM_ENTITIES["The Workshop"]`
- `docs/governance/LOCATION-FUNCTIONS.md` — Job Description
- `docs/governance/TRANCENDOS-MODELS-MATRIX.md` — base tier and variants
- `docker-compose.production.yml` — service `forgejo`
- `deploy/forgejo/` — implementation
- `compliance/magna-carta/compliance/sector_profiles.yaml` — sector activation
