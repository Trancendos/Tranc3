# Solution Pack — The Lighthouse

> **PID-LTH · AID-LTH-01 · Security pillar**
> Generated by `scripts/build_solution_packs.py`. **DERIVED** sections are read
> from the registers named inline and are verifiable. **SCAFFOLD** sections are
> generated starting points shaped by those facts — drafts to accept or replace,
> not descriptions of what exists.

## 1. What this is — DERIVED

| Field | Value | Source |
|---|---|---|
| Location | The Lighthouse | `src/entities/platform.py` |
| Product ID | `PID-LTH` | `_assign_ids()` |
| Lead AI | Rocking Ricki (`AID-LTH-01`) | `src/entities/platform.py` |
| Base model tier | Tranc3 | `get_orchestration_tier()` |
| Job Description | Head of Cryptographic Token Services | `docs/governance/LOCATION-FUNCTIONS.md` |
| Pillar | Security | `Pillar` enum |
| Reports to Prime(s) | The Guardian (Marcus Magnolia) | `primes` |
| Status | ✅ Deployed | `CLAUDE.md` service table |
| Code path | `workers/identity-service/` ✅ on disk | filesystem |
| Port | 8015 | compose / `worker_port` |
| Compose service | `identity-service` | `docker-compose.production.yml` |
| Rollout priority | P2 | CLAUDE.md worker map |

**Role.** Cryptographic token assignment, authenticator, token scanner

## 2. What it does — DERIVED

**Primary function.** Cryptographic Token Applicator

**Abilities**
- Universal Token Genesis: Mints tokens for new platform items.
- Identity Anchoring: Cryptographic signature from birth.

**Operating modes**

- *Online:* Live token minting for new cloud entities/incoming data.
- *Offline:* Offline token generation (syncs/validates upon reconnect).

The offline mode is a design constraint, not a footnote: it states what this
Location must still deliver when its upstreams are unreachable, and any
implementation that cannot honour it is incomplete regardless of test coverage.

## 3. Architectural requirements — DERIVED constraints, SCAFFOLD targets

**Hard constraints — these come from the estate, not from preference.**

- **Build context is `./workers/identity-service`**, so `src/` is *not* in the image. This Location
  cannot `from src.* import ...` — ported logic must be self-contained. This is the
  single most common cause of a worker that passes tests and dies in the container.
- **SQLite over shared state** — each worker owns its own database file (principle 1).
- **In-memory token-bucket rate limiting** — no external KV (principle 2).
- **Zero-cost posture** — no paid dependency may be introduced without funding sign-off.

**Non-functional targets — SCAFFOLD, set these against real measurements.**

| Attribute | Starting target | Why this number needs replacing |
|---|---|---|
| Health endpoint | `GET /health` < 200 ms | compose healthcheck already polls it |
| p95 latency | < 500 ms | placeholder — no load profile exists yet |
| Availability | 99% single-node | one chassis; see `deploy/CITADEL_OPERATIONS.md` §9 |
| Data durability | volume-backed + off-box backup | snapshots are not backups |

## 4. Design schematic — DERIVED topology

```mermaid
flowchart LR
    C[Client] --> A[api.py]
    A --> S[The Lighthouse<br/>8015]
    S --> DB[(SQLite<br/>own file)]
    S -.reports.-> P[The Guardian (Marcus Magnolia)]
    S --> AA[The Minter]
    S --> AB[The Stamper]
    AA -.-> SealBot[Seal-Bot]
    AA -.-> WaxBot[Wax-Bot]
    AA -.-> SignetBot[Signet-Bot]
    AA -.-> SealStampBot[Seal-Stamp-Bot]
    S --> OBS[The Observatory<br/>audit]
```

## 5. Blueprint — SCAFFOLD

| Layer | Component | Note |
|---|---|---|
| Ingress | in-process router | mounted in api.py |
| API | FastAPI app | `/health`, `/status`, domain routes |
| Domain | The Minter + The Stamper | the two Agents below |
| Automation | Seal-Bot, Wax-Bot, Signet-Bot, Seal-Stamp-Bot | the four Bots below |
| Persistence | SQLite | identity-data → /data |
| Observability | structured JSON + W3C trace | `src/observability/tracing.py` |

## 6. Storyboard and schema — SCAFFOLD

A first-run journey, derived from the abilities above. Replace with the real
journey once a user has actually walked it.

```
1. Request arrives  →  api.py routes
2. The Minter — Mints unique cryptographic identifier tokens for all new content.
3. The Stamper — Attaches verified, tamper-evident digital metadata to packets.
4. Bots fire: Seal-Bot, Wax-Bot, Signet-Bot, Seal-Stamp-Bot
5. Result persisted to SQLite; event emitted to The Observatory
6. Response returned with trace_id for correlation
```

**Proposed schema** — shaped by the abilities; not yet implemented.

```sql
CREATE TABLE IF NOT EXISTS the_lighthouse_records (
    id           INTEGER PRIMARY KEY AUTOINCREMENT,
    record_id    TEXT NOT NULL UNIQUE,
    actor        TEXT,
    payload      TEXT NOT NULL,        -- JSON
    state        TEXT NOT NULL DEFAULT 'new',
    created_at   REAL NOT NULL,
    updated_at   REAL
);
CREATE INDEX IF NOT EXISTS idx_the_lighthouse_state ON the_lighthouse_records(state);

CREATE TABLE IF NOT EXISTS the_lighthouse_audit (
    id           INTEGER PRIMARY KEY AUTOINCREMENT,
    record_id    TEXT NOT NULL,
    action       TEXT NOT NULL,
    actor        TEXT,
    at           REAL NOT NULL
);
```

## 7. Components — DERIVED

**Agents (Tier 4)**

| SID | Agent | Responsibility |
|---|---|---|
| `SID-LTH-01` | The Minter | Mints unique cryptographic identifier tokens for all new content. |
| `SID-LTH-02` | The Stamper | Attaches verified, tamper-evident digital metadata to packets. |

**Bots (Tier 5)**

| NID | Bot | Responsibility |
|---|---|---|
| `NID-LTH-01` | Seal-Bot | Locks system-state snapshots, preventing unauthorized modifications. |
| `NID-LTH-02` | Wax-Bot | Generates temporary, single-use visual watermarks for digital assets. |
| `NID-LTH-03` | Signet-Bot | Validates credentials, signing certificates for structural operations. |
| `NID-LTH-04` | Seal-Stamp-Bot | Applies file-system metadata to register the exact creation details. |

## 8. Design direction — SCAFFOLD

**Foundation.** No OSS foundation is recorded for this Location. Either one
should be chosen and added to CLAUDE.md, or the build-from-scratch decision
should be written down with its reasoning.

**Interface tone.** Security pillar. Surface the two Agents as the
primary verbs and keep the four Bots as background automation the user never
has to name.

## 9. Templates — SCAFFOLD

**Health response** (matches `to_health_meta()`):

```json
{
  "location": "The Lighthouse",
  "pillar": "Security",
  "lead_ai": "Rocking Ricki",
  "primes": ['The Guardian (Marcus Magnolia)'],
  "status": "ok"
}
```

**Compose service:**

```yaml
  identity-service:
    build: { context: ./workers/identity-service, dockerfile: Dockerfile }
    environment: [ PORT=8015 ]
    ports: [ "8015:8015" ]
```

## 10. Epics and stories — SCAFFOLD

Sequenced against the readiness gaps below, so the first epic is whatever is
actually missing rather than a generic phase 1.

### Epic 1 — Implement the abilities

- As a user, I can exercise: Universal Token Genesis.
- As a user, I can exercise: Identity Anchoring.
- As an auditor, every action emits an Observatory event carrying `PID-LTH`.

### Epic 2 — Prove it works

- As a maintainer, `tests/` covers The Lighthouse's health, status and each ability.
- As a maintainer, the offline mode above is tested, not assumed.

## 11. Technical debt — DERIVED where evidenced

| Item | Evidence | Impact |
|---|---|---|
| Compose service has no Traefik router | compose labels | Reachable inside the network only — no external route |
| No test files under the code path | filesystem check | Regressions land silently |

## 12. Wireframe — SCAFFOLD

```
┌──────────────────────────────────────────────────────┐
│ The Lighthouse                               PID-LTH │
├──────────────────────────────────────────────────────┤
│ Cryptographic Token Applicator                       │
├──────────────────────────────────────────────────────┤
│  ▸ The Minter               Mints unique cryptograph│
│  ▸ The Stamper              Attaches verified, tampe│
├──────────────────────────────────────────────────────┤
│  bots: Seal-Bot, Wax-Bot, Signet-Bot, Seal-Stamp-Bo │
├──────────────────────────────────────────────────────┤
│  [ health ]  [ status ]  route /the-lighthouse      │
└──────────────────────────────────────────────────────┘
```

## 13. Prioritisation — DERIVED

**Criticality 5/10 · Readiness 8/10 → Invest — above-median dependency, below-median readiness**

Classified against the estate's own medians (criticality 3, readiness
8 across all 43 Locations), not a fixed threshold — the two axes do not
share a scale, so one absolute cut-off would bucket almost everything together.

They are also kept separate rather than multiplied. Collapsing them would
double-count: status and dependency facts feed both terms, so the product
systematically ranks safe-and-unimportant above important-and-unfinished.

| Axis | Score | Reasons |
|---|---|---|
| Criticality | 5/10 | worker-map priority P2 (+2); answers to 1 Prime(s) (+1); Security pillar — platform-wide blast radius (+2) |
| Readiness | 8/10 | status ✅ in CLAUDE.md (+3); code path `workers/identity-service/` exists on disk (+2); at least one Python file present (+1); compose service `identity-service` defined (+2) |

## 14. Documentation — DERIVED

- `PLATFORM_ENTITIES.md` — canonical entry for PID-LTH
- `src/entities/platform.py` — `PLATFORM_ENTITIES["The Lighthouse"]`
- `docs/governance/LOCATION-FUNCTIONS.md` — Job Description
- `docs/governance/TRANCENDOS-MODELS-MATRIX.md` — base tier and variants
- `docker-compose.production.yml` — service `identity-service`
- `workers/identity-service/` — implementation
- `compliance/magna-carta/compliance/sector_profiles.yaml` — sector activation
