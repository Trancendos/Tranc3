# Exact-pinned — Do NOT use >= or ~=
fastapi==0.141.1
starlette==1.6.0
uvicorn==0.52.4
pydantic==2.13.4
redis==8.1.0
duckdb==1.5.5
# diskcache: CVE-2025-69872 / GHSA-w8v5-vhqr-4h9v (MODERATE) — unsafe pickle
# deserialization in diskcache's DEFAULT on-disk format. 5.6.3 is the LATEST
# release and the advisory has no fixed version, so there is no version to bump
# to. MITIGATED IN CODE: worker.py opens the cache with a hardened
# _NoPickleJSONDisk (a JSONDisk subclass) that (a) writes/reads values as JSON
# and (b) hard-rejects the pickle read paths (mode=MODE_PICKLE values and
# non-raw keys) that the base Disk would otherwise pickle.load() — so a tampered
# or legacy pickle row raises (cache miss) instead of executing code. Verified
# with an adversarial MODE_PICKLE injection test. Scanners still version-match
# 5.6.3 (usage-blind), so it stays tracked in .trivyignore.
diskcache==5.6.3
