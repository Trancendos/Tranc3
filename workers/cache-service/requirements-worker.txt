# Exact-pinned — Do NOT use >= or ~=
fastapi==0.136.3
starlette==1.3.1
uvicorn==0.34.3
pydantic==2.11.5
redis==6.2.0
duckdb==1.3.0
# diskcache: CVE-2025-69872 / GHSA-w8v5-vhqr-4h9v (MODERATE) — unsafe pickle
# deserialization in diskcache's DEFAULT on-disk format. 5.6.3 is the LATEST
# release and the advisory has no fixed version, so there is no version to bump
# to. MITIGATED IN CODE: worker.py opens the cache with disk=diskcache.JSONDisk,
# so values are stored/read as JSON and never unpickled — the vulnerable code
# path is not exercised. Scanners still version-match 5.6.3 (usage-blind), so it
# stays tracked in .trivyignore with that justification.
diskcache==5.6.3
