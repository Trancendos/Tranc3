# Exact-pinned — Do NOT use >= or ~=
fastapi==0.136.3
starlette==1.3.1
uvicorn==0.34.3
pydantic==2.11.5
redis==6.2.0
duckdb==1.3.0
# diskcache: CVE-2025-69872 / GHSA-w8v5-vhqr-4h9v (MODERATE) — unsafe pickle
# deserialization. 5.6.3 is the LATEST release and the advisory has no fixed
# version (pickle is intrinsic to diskcache's on-disk format), so there is no
# version to bump to. Mitigated here: the cache dir is an internal container
# volume ($CACHE_DISKCACHE_DIR, default /data/diskcache) that only this worker
# writes; a value is only unpickled on read, and exploiting it requires an
# attacker who can already write arbitrary files into that volume (i.e. prior
# container/host compromise). We only cache trusted, service-generated values —
# never attacker-controlled pickle payloads. Tracked in .trivyignore.
diskcache==5.6.3
