# Exact-pinned — Do NOT use >= or ~=
# Fixes CVE-2024-33663 (algorithm confusion) and CVE-2024-29370 (JWE DoS)
fastapi==0.141.1
starlette==1.6.0
uvicorn==0.52.3
pydantic==2.13.4
python-jose[cryptography]==3.5.0
# ecdsa explicit pin (previously unpinned/transitive via python-jose's
# `Requires-Dist: ecdsa!=0.15`, violating this repo's exact-pinning policy).
# 0.19.2 fixes PYSEC-2026-2467 (DoS via malformed DER signature length field).
ecdsa==0.19.2
# Accepted risk (upstream-blocked, do not re-flag as unfixed): CVE-2024-23342
# (PYSEC-2026-1325) is a Minerva timing side-channel in
# ecdsa.SigningKey.sign_digest() / ECDH; unlike PYSEC-2026-2467 above, no
# patched release exists for this one (fix_versions: [] in the advisory) --
# the ecdsa maintainers have stated side-channel attacks are out of scope and
# there is no planned fix.
# This worker never signs/verifies ES256/ES384/ES512 JWTs (grep confirms no ES2xx usage),
# so the vulnerable ECDSA sign/ECDH code paths are never invoked. See SECURITY.md.
pyasn1==0.6.4
email-validator==2.3.0
pyotp==2.9.0
argon2-cffi==25.1.0
