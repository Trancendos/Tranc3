# Exact-pinned — Do NOT use >= or ~=
fastapi==0.141.1
starlette==1.6.0
uvicorn==0.52.4
pydantic==2.13.4
httpx==0.28.1
# Optional engine SDKs (install only if using that tier):
# temporalio==1.7.1    # Temporal SDK — Tier 4
# luigi==3.5.2         # Luigi in-process — Tier 7
# prefect==3.4.5       # Prefect client — Tier 3 (server runs separately)
